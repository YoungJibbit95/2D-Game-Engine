
# YjsE Capability Matrix

Last updated: 2026-07-22 after Session 0 baseline integration, subsystem audits and isolated Release performance validation

Capability status values are limited to `verified`, `implemented-unverified`, `partial`, `planned`, `blocked`, and `deprecated`. Local evidence is never presented as a hosted Windows/Ubuntu result.

## Validation Baseline

- Audit-start HEAD: `f9a06072a1172d2bd8d064cabad92235443dd8c8` on `master`, with a pre-existing dirty tree.
- Audit-start Debug and Release: builds passed; tests were 459/461 in each configuration.
- Current Debug and Release solution builds pass with 0 warnings/errors. The functional partition passes 1,710/1,710 in both configurations; 21 performance classes containing 39 tests pass in fresh Release processes. Combined-host Debug/Release timing outliers are diagnostic rather than the timing acceptance gate. Focused regressions cover direct/diffuse/lunar solar radiance, local-surface sky portals, mined shafts, temporal reprojection, dynamic projectile lights, finite-vertical parallax, biome-gated snow, physical particles, transactional mana, mobility abilities, deterministic swept projectile hits, wall mining, world-event clock rebasing, developer command intents and cross-chunk tree dependencies.
- Focused gates now also cover deterministic age-first AI decision budgets, full-rate physics under AI overload, pooled graph-alias render targets, descriptor/lifetime safety and bounded continuous body-body TOI with fail-closed pass limits.
- Strict Python main-manifest audit: 190 IDs, 184 canonical PNG sources plus 6 valid source-alias groups and 0 hard issues. The supplemental Wave 05 report covers 206 definitions and 0 hard issues, but records 24 Wave 05 paths outside the CI audit's `sprites/**` disk-scope accounting. Soft review debt remains 104 palette, 32 silhouette and 2 occupancy findings.
- Current four-biome smokes remain green. Fresh integrated Forest V7 Release smokes pass at 1920x1080 and 2560x1440 with 259 resources, 1,514 frames and 0 invalid resources. Visual inspection verifies five independent feature/depth planes, bright open sky, no lower-image repetition and readable actor/UI composition; hosted Windows and Ubuntu remain `implemented-unverified`.

## Matrix

| Capability | Owner project/module | Status | Active runtime usage | Implementation files | Tests | Runtime validation | Performance baseline | Known limitations | Dependencies | Next acceptance criterion |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Exact .NET SDK selection | Repository control plane | `verified` | All local .NET commands | `global.json` | Build/restore gates | Current host selects exactly 8.0.420 with roll-forward disabled | not a performance metric | Hosted selection not observed | SDK distribution/setup action | Windows and Ubuntu CI both report 8.0.420 |
| Locked NuGet restore | Repository control plane | `verified` | All four projects | `Directory.Build.props`, four `packages.lock.json` files | locked restore | Local `--locked-mode` passed | not a performance metric | Hosted feeds/platform graphs unobserved | NuGet sources and exact SDK | Hosted locked restore passes on both OS families |
| Compiler/analyzer/style policy | Repository control plane | `verified` | All C# projects | `Directory.Build.props`, `.editorconfig` | style verify and builds | C# 12, .NET 8 analyzers and warnings-as-errors pass locally | not a performance metric | Gate intentionally verifies C# style, not repository-wide line-ending normalization | locked restore | Hosted style/build matrix passes without suppressions |
| Windows CI | GitHub Actions | `implemented-unverified` | PR, push and manual workflows | `.github/workflows/ci.yml` | Debug/Release focused/full gates | Workflow implemented, no hosted run | unknown | Requires a committed/pushed revision | GitHub hosted runner | First complete green Windows matrix |
| Linux CI | GitHub Actions | `implemented-unverified` | PR, push and manual workflows | `.github/workflows/ci.yml` | Debug/Release plus xvfb smoke | Workflow implemented, no hosted run | unknown | Casing/native/display behavior unobserved | GitHub hosted runner and xvfb | First complete green Ubuntu matrix |
| Renderer-neutral Core boundary | `Game.Core` | `verified` | Core simulation/content/persistence | `Game.Core/**/*.cs`, project graph | assembly/project/source boundary tests plus full suite | No MonoGame/XNA/Client reference found; boundary is automated | unknown | Hosted graph remains unobserved | project graph | Keep boundary gate green as runtime modules expand |
| Local Debug/Release solution | Solution | `verified` | All four projects | `YjsE.sln`, project files | full builds | Post-integration Debug and Release pass with 0 warnings/errors | not a performance metric | Hosted OS evidence separate | exact SDK and locked restore | Same result in hosted matrix |
| Full unit/contract suite | `Game.Tests` | `verified` | Core, client rendering and repository contracts | `Game.Tests/**/*.cs` | focused and full-suite runs | Debug and Release functional coverage each pass 1,710/1,710; 21 timing classes containing 39 tests pass in fresh Release processes. Timing gates keep their original thresholds and are isolated from shared-host scheduler residue | not a performance metric | Hosted Linux/Windows not observed; combined execution remains useful diagnostic evidence but is not the timing acceptance process | solution builds and content | Reproduce the functional plus class-isolated timing partitions across the hosted matrix |
| Project/content packaging | `Game.Client`, `Game.Core.Projects` | `verified` | Normal startup and published smoke | `Game.Client.csproj`, `ClientPaths`, project loaders | project/content tests plus Release publish | Final integrated Release publish and generic content-ID scene matrix succeed | exact final payload inventory was not recaptured | No standalone installer or external-game fixture | project manifest and content loader | Add an external game-pack publish fixture |
| Base/mod sprite source provenance | `Game.Core.Data`, `Game.Core.Assets` | `verified` | Runtime sprite resolution | `GameContentLoader.cs`, `SpriteAssetDefinition.cs` | base override and mod-only integration test | Published smoke uses project loader; per-pack absolute roots preserved | included in preload sample | Symlink escapes are not resolved; mod dependency ordering is still basic | content pack loader | Add multi-mod ordering/dependency tests |
| Texture resource/frame ownership | `Game.Client.Rendering` | `verified` | Tile, entity, item, background, effects and UI draw paths; tile frames use CPU-baked padded atlas pages | registry/resource/frame files, `TextureAtlasPlanner`, `TilemapRenderer` | sharing, fallback, path, disposal, residency and atlas planning tests | Current final Forest smoke loads 259 resources, 1,514 runtime frame descriptors and 0 invalid resources; tile atlas construction occurs during content configuration, never Draw | explicit decoded-source and atlas-page byte telemetry | estimate is not measured GPU RAM; atlas currently covers tile frames only | source provenance and graphics device | Extend compatible packing to entity/UI categories and calibrate GPU residency |
| Texture preload groups/budgets | `Game.Client.Rendering` | `verified` | UI, World, Entities, Backgrounds and Effects use explicit decoded-byte budgets | `ClientTextureRegistry`, `TextureGroups/**`, `PlayingState.LoadContent` | 13 residency/group/LRU/lease tests plus smokes | Deterministic preload, LRU eviction, pinning, alias-shared leases and resident-only Draw lookup pass | per-group resident bytes and eviction telemetry active | budgets are decoded-RGBA estimates rather than measured GPU memory | texture ownership | Calibrate budgets on a production pack and add atlas pages |
| Client startup/nonblank render | `Game.Client`, Core session/world generation | `verified` | Dedicated smoke mode, normal menus and forced biome scenes | `ClientSmokeOptions`, `MainGame`, `PlayingState`, `GameSessionBootstrapper`, `InfiniteWorldChunkGenerator.GetSurfaceHeightAt` | generic safe `--scene-biome` contract, 6 parser cases and 17 focused world/session cases | Parser accepts normalized data-driven biome IDs; smoke suppresses debug/profiler overlays; forced scenes use deterministic local surface height and a bounded 5x5 collision-chunk preload; new sessions start at day midpoint while loaded saves retain stored time; five current local scenes and visual QA pass | texture sample above | Hosted scene evidence remains separate | final Release publish and DesktopGL | Add encounter-focused visual smokes with active entities and combat effects |
| Client resource lifetime | `Game.Client`, game states | `verified` | Shutdown and state replacement | `MainGame.Dispose`, `GameStateManager.Dispose`, state disposal | registry exact-once tests | Published smoke exits cleanly with state/event/GPU wrapper disposal | unknown | Long-session device reset/reload not covered | MonoGame lifecycle | Add repeated state/load-unload stress smoke |
| Streaming and chunk rebuilding excluded from Draw | `Game.Client.PlayingState`, tile renderer | `verified` | Active sideview client | `PlayingState.Update/LateUpdate/Draw`, `TilemapRenderer`, `ChunkRenderCache` | full suite, cache behavior and 38 streaming tests | Camera planning and bounded apply execute in Update; visible dirty chunk commands rebuild under a bounded LateUpdate budget; Draw only consumes prepared cache entries | current V3 traversal averages 0.003 ms chunk preparation; streaming operation/time/byte budgets remain active | calibrated long-session rebuild distribution is still absent | streaming service and chunk dirty flags | Add calibrated long camera/rebuild distributions |
| Background chunk streaming | `Game.Core.World.Streaming` | `verified` | Infinite-world client | `ChunkStreamingService`, compact window request snapshots, jobs, retry policy, telemetry and bidirectional harness | focused streaming contracts, planner/allocation gates and full suite | Cancellable jobs, session tokens, retry/backoff, recovery-safe dirty unload and bounded apply remain active; rectangular required/retain sets avoid per-camera hash tables and Debug workers receive fair settlement time | unchanged-camera plan 664 B; calibration initial window 9.896 ms/1,796,413 B; two 65-position cold p99 runs 99.3-203.3 ms and warm p99 0.014-0.015 ms | hosted/Linux behavior and region-read/generate/apply/save attribution remain unobserved | world/session ownership and existing region format | Add labelled service-phase distributions without regressing compact planning |
| Chunk render-cache hot path | `Game.Client.Rendering` | `verified` | Tilemap renderer | `ChunkRenderCache.cs`, `TilemapRenderer.cs`, atlas planner/telemetry and renderer metrics telemetry | loaded trim, LRU budget, hit stability, atlas packing and renderer counter tests | No per-frame `ToHashSet`, `Keys.ToArray`, LINQ trim allocation or texture readback remains; tile frames resolve into atlas-page source rectangles and MonoGame submission deltas are exported | final 1440p Forest averages 55.53 Draw submissions/binding changes and 0.296 ms Tilemap Draw at 0 B with the expanded 1,308-frame content set | GPU time is unsupported by MonoGame metrics; chunk geometry is still SpriteBatch command data rather than persistent vertex/index buffers | active world renderer | Add render-target/geometry-backed chunk batches and compare long traversal distributions |
| Authoritative Core simulation host | `Game.Core.Runtime`, `PlayingState` | `verified` | Active sideview client submits buffered commands/item-use requests to its session simulation | `GameSimulation`, `GameFrameSnapshot`, `EntityFrameSnapshotBuilder`, `ImmutableSnapshotList`, `LivingWorldRuntime`, `LoadedGameSession`, `PlayingState` | ordered phases, immutable history, AI-field preservation, 0/500-entity distributions, replay and full suite | One fixed-tick host owns authoritative state. Inventory/farm caches, columnar copy-on-change snapshots and caller-owned pickup/combat query workspaces preserve immutable historical frames including faction/AI target/telemetry | Session 0 quick: 0.309 ms average/0.570 ms p95/2.756 ms p99 and 3,417 B average allocation; retained calibration: 0.102 ms/0.257 ms p99 and 2,690 B; exact 240-tick replay ends at `0x2B10E91090EC0C22` | no previous-frame interpolation; remaining snapshot columns miss ideal 0 B; the v3 quick scenario is not a speedup comparison to retained v2 data | completed streaming boundary | Reduce remaining snapshot columns and design interpolation without mutable leakage |
| Core lighting plus 2D presentation lighting | `Game.Core.Lighting`, weather/ambient state, client presentation renderer | `verified` | Fixed simulation resolves visible-first dirty chunks; local surface state separates atmosphere/cave ambience; LateUpdate prepares world-reprojected masks and Draw only composites prepared maps | `SolarRadianceModel`, `LightingSystem`, `LightingRenderer`, `TileRayCastShadowMaskBuilder`, `LightingTemporalStabilizer`, `VisibleLightCollector`, `ReflectionRadianceMapBuilder`, `PixelAtmosphereRenderer` | direct/diffuse solar, open-sky/shaft/room occlusion, stale-light invalidation, supercover point rays, temporal reprojection/disocclusion, dynamic projectile lights, reflection and 0 B performance gates | Direct and diffuse solar energy drive an O(mask-pixels) directional transport scan, local AO, finite shadow decay and multi-sample point-light penumbra. World-space history rejects disocclusions instead of smearing mined edges; moving projectiles contribute bounded colored lights, while Beer-Lambert absorption and Schlick weighting feed wet-surface radiance | 104x58 Release fixture averages about 1.05 ms at 0 B. The 600-frame V7 trace averages 0.602 ms mask and 0.207 ms temporal stabilization, both 0 B; total lighting preparation averages 1.505 ms | hardware RT/path tracing, material normal/transmission maps, authoritative RGB light and backend GPU timestamps remain open; rare OS/upload tails remain visible | tile/biome definitions, streaming lifecycle, settings and prepared resources | Add material-aware occluder/transmission/normal contracts and a room/portal skylight cache, then implement backend timestamp queries |
| Screen-space reflections and UI backdrop effects | `Game.Client.Rendering.Effects`, `Game.Client.UI` | `verified` | Playing scene capture composites water/wet surfaces and feeds a quality-scaled prepared Kawase backdrop; lighting contributes a low-resolution colored radiance layer | `ScreenSpaceEffectsRenderer`, `TransientRenderTargetPool`, `BackdropBlurPlanner`, `WaterReflectionSurfacePlanner`, `ReflectionRadianceMapBuilder`, `PresentationWorkScheduler`, `PresentationFrameBudget`, `UiTheme`, `PlayingState` | surface/radiance/blur planning, target descriptor/lifetime/alias safety, quality, pass budgets, cadence, frame-budget, UI theme and interaction tests | Scene/ping/pong graph aliases now bind through a fixed-capacity physical target pool. Compatible quality reconfiguration reuses targets; resize, device change, disable and disposal invalidate them safely with logical/physical/alias/byte telemetry | Acquire plus Bind measures 0.300 us p50, 0.500 us p95/p99 and 0 B across 10,000 iterations; High 1080p radius-8 planning still reduces estimated samples by 62.5% | pool ownership is local to screen-space effects rather than the complete Playing graph; screen-space data cannot reflect off-screen geometry and GPU timing remains unknown | presentation settings, render graph and graphics device | Lift physical descriptor ownership to the global graph, share the surface plan and capture the 1440p tier matrix |
| Biome liquid surface presentation | `Game.Client.Rendering`, `PlayingState` | `partial` | LateUpdate prepares coalesced body, depth, surface and left/right shore commands; Tilemap Draw consumes the bounded plan with point sampling | `LiquidPresentationPlanner`, `WaterPresentationPalette`, `TilemapRenderer`, `PlayingState` | command classification/coalescing, capacity and 0 B focused contracts plus Release runtime smokes | Forest/Amber/Marsh/Crystal palettes share the reflection surface contract instead of creating a second water simulation | 1080p LiquidPrepare averages 0.253-0.384 ms and 0 B; Amber 3440x1440 averages 0.668 ms and 0 B | deterministic spawn captures do not contain a visible water body, so visual shore/depth acceptance is not yet established | prepared tile commands, biome state and reflection planner | Capture water-heavy Amber/Marsh fixtures at 1080p/1440p and accept shore/depth readability visually |
| Modern pointer/gamepad UI interaction | `Game.Client.UI`, menu states, settings | `partial` | Main menu, world select/create, pause/settings, responsive HUD and overlays share control/theme contracts; Escape dismisses the topmost gameplay overlay before pause routing | `UiInteraction`, `UiTheme`, `PixelUiPrimitives`, `AdaptiveOverlayLayouts`, `MainMenuBackgroundPresentation`, overlay states | client UI suites, 47 focused Adventure/tree/terrain layout tests and four current 1080p smokes | The project-owned 1672x941 panorama stages the main menu. Shared carved-wood, inset-slate, brass-fitting and parchment hierarchy reaches HUD, pause/settings, inventory, crafting, character editor and developer surfaces. Expanded 1080p layouts are validated | layout/status/crop planning remains 0 B in steady-state tests; 1080p Main Menu, Gameplay, Pause and Crafting load 263 resources/1,518 frames | `Video.UiScale` is persisted but not applied as a shared viewport transform or inverse pointer transform; Pause accepts 0.5-4.0 rather than the intended 0.75-2.0; 320x180 Main Menu content overflows; no screenshot-diff suite | input snapshots, authoritative player stats, settings persistence and state-owned textures | Apply one bounded UI scale/inverse-pointer contract, add deterministic Inventory/Crafting smoke hooks and capture 720p/1080p/1440p matrices |
| Fixed-tick layered character animation | `Game.Core.Animation`, content loader, client character/entity renderers | `verified` | Active player consumes the game-owned `player.wave06` rig/state/action profile; active entity preparation resolves the same central runtime registry once at content load | animation content definitions/registry/JSON loader, `GameContentDatabase.RuntimeAnimations`, `Wave04PlayerCharacterRenderer`, `RuntimeEntityVisualProfileCatalog`, `wave06_character_runtime_profiles.json` | animation/content/renderer subsets plus full Debug/Release suites | Five synchronized native 24x40 layers expose 16 source-rectangle poses without legacy upscaling; body, clothes, hair, armor and equipment stay on one fixed-tick animation clock | command capacities are fixed; render-frame GPU batches remain unknown | renderer owner retains its legacy Wave04 class name and encounter screenshots do not yet prove every action state | sprite manifest and fixed simulation | Rename the compatibility renderer owner and add encounter captures for all Wave 06 actions |
| Scalable procedural backgrounds and particles | `Game.Client.Rendering`, `Game.Core.Particles` | `verified` | Active Playing background uses two V7 feature planes plus Far/Mid/Near depth planes; feedback debris uses the shared renderer-neutral particle physics pool | `ParallaxBackgroundRenderer`, `ParallaxLayerPlanner`, `ParallaxViewportLayoutPlanner`, `WeatherParticlePresentation`, `GameplayParticleSystem`, `ParticlePhysicsWorld`, `BackgroundFeaturesV7/**` | current 1,749-test Debug/Release suites cover fullscreen five-plane composition, ultrawide/negative-X invariance, no bottom repetition, premultiplied/clipped snow, underground suppression, collision/budget/determinism and 0 B hot paths | Fullscreen feature/depth projection removes the former image-bottom replication. Snow is clipped and premultiplied instead of drawing a white field. Tile-break and combat debris use deterministic semi-implicit motion, gravity/wind/drag, swept tile collision, bounce/fricti…749 tokens truncated…ap Forest traces average 6.061/6.081 ms at 1080p/1440p with 6.062/6.062 ms p95 and 8.716/7.520 ms p99. The rolling windows keep 98.83/99.22% inside 120 Hz and 98.83/97.66% inside 144 Hz; CPU Draw averages 0.603/0.642 ms | intervals are limiter/OS observations rather than GPU timestamps; every-frame 165 Hz is not claimed and rare 20 ms host tails remain | MonoGame timing, Windows scheduler and video settings | Add refresh enumeration/backend timestamps and run the same long gate at 1440p across all quality/biome combinations |
| Friendly/hostile entity ecosystem | `Game.Core.Entities`, AI, spawning and loot | `verified` | Offscreen activity-source ingress and faction actors consume authoritative player/living-world state | `EntityAiDecisionScheduler`, `EnemyEntity`, `EntityManager`, `Entities/AI/**`, `Spawning/**`, `EntityAttackSystem`, `EntityDeathLifecycle`, `Loot/**` | loaded-chunk/solid placement, encounter fallback, overload/fairness/elapsed-time contracts, repeated 18,000-tick streaming soak and 500/2,000-entity gates | A fixed-capacity top-K scheduler budgets behavior decisions with deterministic tick/ID ties, age-first fairness and distance/engagement priority. Health, status, lifecycle and Physics still execute every authoritative 60 Hz tick, deferred actors receive their full accumulated decision delta and no cached velocity overwrites impulses | isolated Release 500: 0.995 to 0.371 ms average (-62.7%), 0.886 ms p99, 0 B/tick; 2,000: 4.557 to 1.378 ms average (-69.8%), 3.132 ms p99, 0 B/tick | sensors still run synchronously inside selected decisions; full biome encounter matrix, jobified perception and a global navigation graph remain absent | living-world snapshot, named RNG, loaded chunk residency and central Physics | Add bounded perception/navigation work queues and dense combat captures while retaining deterministic fairness and 0 B scheduling |
| Regional world/weather/event contracts | Core generation, biomes, weather and world events | `verified` | `LivingWorldRuntime` owns the advanced deterministic executor, advances scheduled events at most once per 60 ticks and evaluates successful player actions exactly once | `LivingWorldRuntime`, `WorldEvents/**`, `WorldEventStateSaveService`, `GameContentDatabase.WorldEvents`, regional planner/profile/registry, infinite generator | runtime throttle/phase/journal, sidecar recovery/migration/resume, exact-once scheduled/action activation, rare-loot, replay and feedback suites | Active event, cooldowns, bounded journal and last action sequence persist atomically; scheduled/action activation, modifiers and normal/rare drops route exactly once into bounded visual/audio cues | planner trace covers 4,097 positions; current 256x128 simple-generation quick avg 0.694 ms | trigger/room zones and production audio remain open | content loader, world time, named RNG, saving and GameSimulation | Add dynamic trigger/room zones and authored event encounters |
| Developer console and typed commands | Core commands/developer tools plus client overlay | `verified` | Playing adapter exposes execution, completion selection, separate history/output navigation, scrolling and help | command/developer-tool files, console adapter/model/overlay | 23 client/UI tests plus command suites | Published Playing smoke opens and renders the console at 640x360 | stable-model tests cover key UI paths | some intents need richer domain-specific output | command context and session | Add scripted execution smoke for every mutating command family |
| Download and engine-doc website | `Website/**` | `verified` | Svelte 5/Vite product page is separated from the 26-article searchable engine wiki and emits two portable static entry points | Svelte components, compiled local bundles, JSON data, update/build/validation tools and Lucide icons | `svelte-check`, optimized build, source/bundle/data/local-link/PNG/provenance validators | 0 Svelte diagnostics; static gate passes 26 docs, 8 built references, 41 PNGs and 30 byte-exact Game.Data copies; search, filters, Deep Links, disabled-download truth and responsive contracts are component-owned | gzip: shared runtime 21.54 kB, home 7.55 kB, docs 4.75 kB, CSS 7.97 kB; no CDN runtime | no hosted URL or downloadable artifact; current Svelte snapshot could not be browser-captured under the active local-browser policy | validated release artifacts and ledger | Run hosted/mobile visual QA, then publish the first signed artifact and activate its honest download entry |
| Deterministic runtime RNG/replay | Core runtime/gameplay | `verified` | Session-owned named streams drive combat, status, spawn, farming, event actions and death keys; optional simulation capture records authoritative inputs | `Randomness/**`, `Diagnostics/Replay/**`, `random-state.json`, `world-events.json`, simulation/save/load/bootstrap, state hasher | 20 replay-contract tests plus isolation, recovery, mid-trace resume and dual-session replay | Versioned bounded input frames include delta, player/item input and checkpoint hashes; JSON has a 64 MiB cap and first-divergence reports order/input/checkpoint/hash/version plus last matching checkpoint | calibration state hash avg 0.974 ms; 1,200-tick dual replay avg 239.922 ms | replay recording is an explicit diagnostic session and is not yet persisted as a save sidecar | authoritative simulation and additive save sidecars | Add playback orchestration, user-facing export and save-linked replay metadata |
| Core fixed-tick allocation budget | `Game.Core.Runtime` | `partial` | Representative session with living world, guard/combat and active AI | benchmark, phase allocation export, immutable snapshot ownership, columnar entity builder and query buffers | calibration, determinism, immutable historical frames, detailed AI fields, 0/500-entity and full-suite gates | quick thresholds pass; pickup magnetism, combat/projectile queries, representative entity update, immutable enumeration, attack sequencing, feedback draining, flags and spawn-cap maintenance are 0 B steady state | latest quick 0.116 ms/3,125 B; calibration 0.102 ms/2,690 B | `FrameSnapshot` remains 2,419 B quick/1,621 B calibration; interpolation remains open | authoritative simulation host | Reduce remaining snapshot columns and add previous-frame interpolation |
| Content loading/cross-reference validation | `Game.Core.Data` | `verified` | Project bootstrap, benchmark and smoke | loaders, registries, validator and `AnimationContentJsonLoader` | post-integration Debug/Release content and full suite | Central loading includes runtime clips, rigs, state machines, character actions, entity visual profiles and player-action event triggers with deterministic base/mod overrides and diagnostics alongside all existing content | current quick load avg 56.547 ms and 2,778,368 B average | representative multi-mod set and external JSON schema gate are missing | project/content paths | Add deterministic representative mod-pack benchmark/validation and schema diagnostics |
| Sprite manifest/brief/audit contract | `Game.Data`, `Game.Core.Assets` | `partial` | CI/tooling and client preload | manifests, briefs, Python audit v2, C# asset tests | asset/content suites plus strict Python gates | Main scope passes at 190 manifest entries/184 PNGs/0 hard issues; supplemental V6/Wave06/Terrain/foliage/trunk/mobility/background-feature packs pass 12/12, 5/5, 6/6, 9/9, 3/3, 4/4 and 8/8 with 0 hard issues. The V7 brief schema and lowercase content paths are validated by the full repository suite | generated runtime packs carry exact dimensions, prompt/specification, deterministic generator, preview and provenance | Wave 05 remains outside part of the canonical `sprites/**` review inventory and soft cross-wave style debt remains | style contract and Pillow | Normalize Wave 05 and add a cross-wave style/scale audit rather than only file-contract validation |
| Representative production asset waves | layered player, biome backgrounds, terrain, ambience/weather/combat effects and UI | `partial` | Wave 06 player, V6/V7 backgrounds, Terrain Polish V1, Tree V3/V5, Mobility Accessories V1 and Legacy Boar Polish V1 are active | production PNGs, deterministic generators, manifests, briefs, prompts/specifications, provenance, audits and previews | visual asset audits plus focused runtime/content/animation/UI tests | Four biomes bind five authored-distance planes; three mobility items and one shared HUD sheet bind equipment abilities; native boar/rabbit silhouettes are visible in the latest Forest scene. The 1920x1080 V7 smoke proves 259 resources/1,514 frames with no invalid resource | current Forest gameplay capture is green; the full biome/ability/combat action matrix remains incomplete | remaining legacy low-detail sprites, production audio and broader biome variation remain | texture groups, content registries and parallax composition | Capture V7 mobility/combat scenes in every biome, then polish the next legacy enemy/item/UI family |
| Foreground tree silhouettes and biome identity | `Game.Core.World.Generation`, biome content | `verified` | Finite and infinite world generation share deterministic tree geometry, biome materials and renderer-selected foliage atlases; foliage schedules bounded falling leaves into the existing particle pool | `TreeSilhouettePlanner`, `TreeMaterialResolver`, `TreeTileVisualSelector`, `TreeAutoTilePresentation`, `FallingLeafPlanner`, generation | 26 focused selector/silhouette/biome tests plus strict foliage assets and full content contracts | Twelve seed/anchor-stable silhouettes combine root flare, stepped branches and overlapping dense crown lobes. One tree-wide palette now covers trunk and every regional canopy cell; authored oak/autumn/marsh lighting direction is retained and only generic legacy leaves may mirror | generation/selection remain deterministic and 0 B; the current V5 Forest capture removes cross-sheet canopy patchwork without adding textures or draw calls | native overhang overlays, richer trunk/decor sockets and seasonal comparison remain open | tile registry, biome profiles and fixed particle capacity | Add bounded trunk/decor and native canopy-overhang commands without regressing atlas batches |
| Adventure terrain surface presentation | `Game.Client.Rendering`, tile definitions | `verified` | Tilemap Draw emits bounded presentation-only grass fringe, roots, facets, accents and hanging moss after cached base tiles | `TerrainSurfaceDetailRenderer`, `TilemapRenderer` | deterministic exposure/material tests, negative-coordinate stability, 0 B command generation and current runtime smokes | Grass, dirt, stone, amberstone, marsh moss, snow and ice gain material-aware sub-tile depth without mutating authoritative tiles. All pixel primitives are emitted in one second pass after texture buckets, avoiding per-tile texture alternation | Forest before/after: Render.Tilemap 2.963 to 0.368 ms and average Draw submissions 1,976 to 69; command generation is allocation-free | current accents are code-native; authored ruins/waterfalls/bridges and larger terrain overlays remain partial | cached tile commands, pixel texture and exposure queries | Add bounded authored set-piece/decor commands and verify all-biome 1080p/1440p batches |
| Mining/building/interaction transactions | `Game.Core.Interaction`, `GameSimulation`, client feedback | `verified` | Active placement is transactional; mining targets foreground tiles or background walls through one candidate/progress path | `MiningSystem`, interaction query/candidate factory, building validator/system/transaction service | tile/wall mining, active building, atomic inventory/world, negative-X and feedback suites | `GlobalDurationFactor = 0.25` makes valid mining four times faster without bypassing hardness/tool checks. Empty foreground cells expose mineable walls, remove the wall on completion and return typed drops; building retains reach/support/collision and rollback-safe inventory commits | no calibrated interaction CPU trace | furniture anchors, richer wall/liquid placement and server reconciliation IDs remain open | world edits, inventory and item definitions | Add authoritative request/rollback IDs, then furniture/wall/liquid placement rules |
| Inventory/crafting transactions | `Game.Core.Inventory`, `Game.Core.Crafting`, client overlays | `verified` | Active sideview inventory and crafting UI | transaction/query/organization/batch/search/tracking services | focused inventory/crafting/save contracts plus full suite | Atomic batch craft, max craft, favorites, cursor return, equipment preflight and typed result events pass locally. Inventory/Crafting consume shared compact/regular/expanded Adventure-shell planners; compact mode stacks details and removes fixed-width overflow | recipe refresh is revision-cached and the Draw path no longer sorts station sets or uses ingredient LINQ; direct open-overlay screenshot automation remains absent | Recipe unlock/pin session persistence and overlay interaction automation remain incomplete | item definitions, save service and UI adapter | Persist knowledge/pins and add overlay input tests |
| Save/load round trips | `Game.Core.Saving` | `verified` | Session/world persistence | saving/session services, `SimulationSaveService`, `RandomStateSaveService`, `WorldEventStateSaveService` | save/migration/hash/RNG/event recovery tests | Negative X, player v1-v3, simulation time, RNG and world-event sidecars round-trip; world metadata V3 persists generation version/profile and missing legacy fields select V1; event state uses atomic replace, backup recovery, future-version rejection, legacy defaults and registry normalization for removed mods | save time/bytes unknown | region overwrite recovery, save rotation and user-facing corruption reports remain incomplete | content registries and filesystem | Add interrupted region-write recovery, integrity reports and save rotation |
| Reusable 2D movement and physics | `Game.Core.Movement`, `Game.Core.Physics`, client camera | `verified` | Buffered/coyote/variable-height sideview intent supports wall/double jumps, timed flight and glide; central Physics owns player/enemy/item/projectile bodies and client camera follows through a deadzone | movement controllers/options, `PhysicsWorld`, `TileCollisionResolver`, `EntityPhysicsRuntime`, `Camera2D` and contracts | controller/ability boundaries, camera deadzone/damping/teleport, shape/contact isolation, continuous-body and performance gates | Coyote/buffer/jump-cut make ground jumps forgiving. Equipment stats grant one resettable air jump, bounded powered flight and gravity/terminal-speed glide. Camera tracking uses critically damped frame-rate-independent response, X/Y deadzones, bounded velocity look-ahead and teleport snap. Body response applies mass/resistance-aware clamped knockback | 500 fast bodies: 0.665 ms/step and 0 B; dense 128-body/8,128-pair fixture: 6.311 ms/step and 0 B; ability/camera focused suites pass | inverted slopes, arbitrary polygons, joints, liquid forces and production partial-tile authoring remain open | tile flags, collision, equipment stats and fixed update | Integrate authored partial tiles and tune mobility/knockback in encounter captures |
| Active-cell liquid simulation | `Game.Core.World.Liquids` | `verified` | Authoritative world simulation tracks bounded active and deferred cell frontiers | `LiquidSimulationSystem`, `LiquidSimulationWorkspace`, scheduler integration | flow correctness, negative-X/infinite-world, active frontier, scan/recovery budget and performance contracts | Steady updates avoid full-region scans; initial and compatibility regions seed under a hard budget; scheduler world rebinding and capacity-recovery regions are explicit | 128 active cells p50/p95/p99 0.021/0.047/0.069 ms and 0 B/step; compatibility probe p99 14.597 ms | pressure/equalization, multiple liquid materials and presentation coupling remain open | tile mutations and scheduler | Activate directly from mutations, then add pressure/equalization and surface snapshots |
| Finite generation workspace | `Game.Core.World.Generation` | `verified` | Finite terrain/cave/ore/wall/liquid/tree/structure passes share chunk-local bounds-safe mutation storage | `WorldGenerationWorkspace`, mutation helpers and generation steps | workspace bounds/materialization/dirty-state, unchanged hashes and 256x128 performance gate | Initial generation bypasses runtime dirty propagation and publishes clean chunks only after all passes complete | latest quick fixture 0.694 ms average/0.862 ms p95 and 1,252,512 B, down from 28.044 ms/17,052,112 B | regional/infinite generation distributions and larger-world memory curves remain open | finite world and generation profiles | Capture 1K/4K world distributions and extend the workspace contract to regional jobs |
| Shared Terraria-style surface topology V2 | `Game.Core.World.Generation`, saving/session bootstrap | `verified` | New finite and infinite worlds share broad deterministic surface and soil sampling; existing infinite saves retain their original generator | `WorldSurfaceSampler`, `TerrainGenerationStep`, `InfiniteWorldChunkGenerator`, `WorldGenerationVersions`, world metadata V3 | multi-seed slope/plateau/relief, finite/infinite coordinate parity, coherent dirt, legacy sampler and save-migration tests plus full suites | 192/64/20-tile quintic surface bands create broad hills, valleys and plateaus with at most one tile local slope in the gate; the spawn sanctuary is flat and the 72/24-tile soil bands remove per-column dirt flicker. Metadata persists generator version/profile; V2 applies only to new worlds and metadata without a version loads as V1 | pure samplers are allocation-free by construction; larger infinite topology distributions remain unmeasured | cave networks, lakes and biome transition elevation still use older separate paths; legacy saves without a stored profile fall back to the selected profile | profile registry, saves, streaming and local-surface resolvers | Add shared chunk-cell cave worms, depression-planned lakes and 32-seed topology distributions without changing V1 generation |
| Caller-owned combat spatial queries | `Game.Core.Combat`, `Game.Core.Entities`, `Game.Core.Projectiles` | `verified` | Projectile/melee/contact resolution and homing reuse indexed workspaces; projectile tile contact uses exact swept AABB over tiles and walls | `CombatQueryWorkspace`, `EntityQueryWorkspace`, `ProjectileEntity`, entity spatial index | swept entity/tile/wall correctness, TOI ordering, dense homing fairness and 500-projectile performance | Projectile hits use deterministic earliest-time ordering, owner/faction filtering and bounded pierce; homing rotates fairly under fixed query/candidate/entry-test limits | 500 projectiles average 0.290 ms, p99 0.532 ms and 0 B across 180 resolutions | body-sized shapes and dense multi-faction distributions remain open | entity manager and spatial index | Add adversarial dense-scene distributions and complete terminal feedback integration |
| Benchmark executable | `Game.Benchmarks` | `verified` | CI quick smoke, local calibration and BenchmarkDotNet categories | stable harness, phase allocation export and `EngineBenchmarks.cs` | executable quick/calibration, streaming BDN dry/short, immutable query and lighting runs | Session 0 v3 quick fixed tick is 0.309 ms average/0.570 ms p95, 2.756 ms p99 and 3,417 B average; retained calibration remains 0.102 ms/2,690 B; typed events remain 0 B and the 240-tick replay matches at `0x2B10E91090EC0C22` | compact streaming dry allocations are 13,576,584 B cold/7,475,264 B warm; lighting remains 40 B for 4/12 chunks | scenario versions and dry/short timing modes are not interchangeable; no backend GPU benchmark or automatic baseline-comparison gate yet | stable fixtures | Add threshold comparison in CI and service-phase distributions |
| Performance budgets | Engineering control plane | `partial` | Acceptance decisions | `PERFORMANCE_BUDGETS.md`, benchmark/smoke artifacts | evidence review | Snapshot, renderer counters, cold/warm streaming, 500-entity, active-liquid, generic physics, finite-generation and combat-query evidence are current; unknown fields remain explicit | see budget document | save, GPU timing/memory, large regional generation and advanced-liquid distributions remain unknown | telemetry and representative fixtures | Measure large-region generation, liquid pressure and dense adversarial combat scenes |
| Durable resume contract | Repository control plane | `verified` | Every future `Weiter` session | `AGENTS.md`, `WORK_LEDGER.md` | document consistency review | Ledger names one exact next action and validation set | not a performance metric | Requires disciplined updates after every session | source-of-truth order | Resume at Epic 2 Milestone 5 without replanning |
## V8 Verification Addendum

| Capability | Owner project/module | Status | Active runtime usage | Implementation files | Tests | Runtime validation | Performance baseline | Known limitations | Dependencies | Next acceptance criterion |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Finite vertical parallax projection | Game.Client.Rendering | `verified` | Every Playing background plane | ParallaxBackgroundRenderer, ParallaxCompositionPlanner, ParallaxLayerPlanner, ParallaxVerticalCoveragePlanner, ParallaxViewportLayout | projection/layout/regression matrices | Day, night, Frostwood and 3440x1440 captures have no terminal-row smear, vertical repetition or cutoff | final background scope remains below 0.1 ms average and 0 B in retained long traces | authored Frostwood-specific planes are still needed | background manifest and living-world presentation ID | Capture every biome at 720p/1080p/1440p |
| Biome-gated frozen weather | Game.Core.Weather, Game.Core.Biomes, client particles | `verified` | Living-world weather resolution and Playing presentation | WeatherState, deterministic weather system, Frostwood biome/profile, weather particle presentation | eligibility, transition and presentation regression matrices | Snow/blizzard occur only where the resolved biome permits frozen precipitation; Frostwood terrain uses snow/ice and the former opaque weather rectangle is absent | bounded particle pool; no fullscreen weather texture allocation | localized accumulation and melt are not implemented | living-world profile and particle capacity | Add accumulation as authoritative biome surface state |
| Local-surface solar and sky-portal lighting | Core lighting plus client presentation | `verified` | Core dirty-light work and per-frame prepared visible mask | SolarRadianceModel, LightingSystem, TileRayCastShadowMaskBuilder, LightingTemporalStabilizer, InfiniteWorldChunkGenerator surface resolver | day/night/shaft/room/negative-X/allocation matrices | Open day is bright, night has blue lunar residual, caves decay from portals and mining rejects stale history | post-V8 1080p mask 1.563 ms average, 2.622 ms peak and 0 B; total prepare 1.971 ms average | material transmission/normal maps and backend GPU timestamps remain open | local terrain planner, dirty chunks and visible mask buffers | Run long 1440p all-biome torch/water/snow matrix |
| Transactional mana and equipment mobility | Game.Core.Combat, Game.Core.Movement, items/equipment | `verified` | Magic item use, projectiles, player movement and HUD | ManaComponent, item-use/attack sequence runtime, SideViewCharacterController, equipment stat resolver | reserve/commit/refund, magic damage, double-jump/flight/glide tests | Failed casts refund reservations; accepted casts commit once; equipment grants one resettable air jump, bounded flight and glide | mobility planning and relevant hot paths are 0 B in focused gates | encounter balance and richer VFX/audio remain open | item definitions, equipment stats and fixed tick | Capture and balance each ability in combat encounters |
| Deterministic projectile/wand contact | Game.Core.Combat, Game.Core.Projectiles | `verified` | Melee/ranged/magic authoritative damage | CombatSystem, ProjectileEntity, ProjectileRuntimeState | entity/tile/wall ordering, high-speed sweep, damage-type and knockback tests | Background walls no longer consume shots; foreground solids do; earliest entity/tile TOI and incoming velocity are preserved deterministically | 500-projectile combat 0.608 ms p99 and 0 B; physics sync 0.687 ms p99 and 0 B | production hit VFX/audio matrix remains open | spatial index, collision shapes and faction rules | Add terminal visual/audio combat capture matrix |
| Developer command palette and typed intents | Core commands/developer tools plus Game.Client | `verified` | In-game developer menu and console | CommandRegistry, CommandSuggestionService, DeveloperConsoleAdapter, DebugConsoleOverlay, PlayingState.DeveloperCommands | parser/help/autocomplete/menu/routing suites | Mouse categories/search/details/examples, command/content autocomplete, history/output filters and typed world/game/render intents are integrated | stable query/model loops remain bounded; no gameplay authority is duplicated in UI | final multi-resolution screenshot smoke is pending | authoritative session and content registries | Capture and click-test 720p/1080p/1440p palette |
| Restored world-event clock normalization | Game.Core.Runtime, world events | `verified` | Session restore and forced time changes | LivingWorldRuntime.AlignWorldEventClock, event executor/state | active duration, cooldown, journal and constructor restore regressions | Rewinding the world clock preserves relative event progress and cannot throw a backwards-advance exception | once per restore/rewind, outside hot Draw path | wall-clock/calendar events are not supported | persisted simulation and event sidecars | Add save/load fuzzing around event phase boundaries |
| Cross-chunk tree/cache dependencies | generation plus client chunk cache | `verified` | Infinite tree silhouettes and tile cache rebuilds | TreeSilhouettePlanner, TreeTileVisualSelector, ChunkRenderCache | boundary material/socket, unload and dependency-stamp tests | Canopy/trunk identity survives chunk boundaries; adjacent caches invalidate when a dependency unloads or changes | steady visible caches avoid rebuild and allocation | native overhang/decor overlays remain open | loaded-neighbor state and tile registry | Add authored branch/decor sockets without increasing atlas submissions |
