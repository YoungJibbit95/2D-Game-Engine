# YjsE Capability Matrix

Last updated: 2026-07-15 after recovery-safe world-event persistence, exact-once gameplay triggers, explicit sky exposure and active entity-animation integration

Capability status values are limited to `verified`, `implemented-unverified`, `partial`, `planned`, `blocked`, and `deprecated`. Local evidence is never presented as a hosted Windows/Ubuntu result.

## Validation Baseline

- Audit-start HEAD: `f9a06072a1172d2bd8d064cabad92235443dd8c8` on `master`, with a pre-existing dirty tree.
- Audit-start Debug and Release: builds passed; tests were 459/461 in each configuration.
- Current post-integration Debug and Release builds/full suites pass with 0 warnings/errors and 995/995 tests in each configuration. The current implementation scope passes style verification; current scene-smoke evidence is recorded below.
- Focused gates cover UI interaction, layered animation, guard/projectiles, activity-source spawning, 200-entity AI/spawn scale, structures, lighting/reflections, audio, living world, RNG/save resume, texture residency and content separation.
- Strict Python main-manifest audit: 180 IDs, 174 canonical PNG sources plus 6 valid source-alias groups and 0 hard issues. The supplemental Wave 05 report covers 204 definitions and 0 hard issues, but records 24 Wave 05 paths outside the CI audit's `sprites/**` disk-scope accounting. Soft review debt remains 100 palette, 32 silhouette and 2 occupancy findings.
- The current isolated Release smokes pass for MainMenu and forced Forest, Amber Grove, Twilight Marsh and Crystal Depths with 199 resources, 1,062 frames and 0 invalid resources. Fresh post-fix Forest and Crystal Depths frame-60 captures confirm a bright surface, readable underground grade and the loaded data-driven player rig; hosted Windows and Ubuntu remain `implemented-unverified`.

## Matrix

| Capability | Owner project/module | Status | Active runtime usage | Implementation files | Tests | Runtime validation | Performance baseline | Known limitations | Dependencies | Next acceptance criterion |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Exact .NET SDK selection | Repository control plane | `verified` | All local .NET commands | `global.json` | Build/restore gates | Current host selects exactly 8.0.420 with roll-forward disabled | not a performance metric | Hosted selection not observed | SDK distribution/setup action | Windows and Ubuntu CI both report 8.0.420 |
| Locked NuGet restore | Repository control plane | `verified` | All four projects | `Directory.Build.props`, four `packages.lock.json` files | locked restore | Local `--locked-mode` passed | not a performance metric | Hosted feeds/platform graphs unobserved | NuGet sources and exact SDK | Hosted locked restore passes on both OS families |
| Compiler/analyzer/style policy | Repository control plane | `verified` | All C# projects | `Directory.Build.props`, `.editorconfig` | style verify and builds | C# 12, .NET 8 analyzers and warnings-as-errors pass locally | not a performance metric | Gate intentionally verifies C# style, not repository-wide line-ending normalization | locked restore | Hosted style/build matrix passes without suppressions |
| Windows CI | GitHub Actions | `implemented-unverified` | PR, push and manual workflows | `.github/workflows/ci.yml` | Debug/Release focused/full gates | Workflow implemented, no hosted run | unknown | Requires a committed/pushed revision | GitHub hosted runner | First complete green Windows matrix |
| Linux CI | GitHub Actions | `implemented-unverified` | PR, push and manual workflows | `.github/workflows/ci.yml` | Debug/Release plus xvfb smoke | Workflow implemented, no hosted run | unknown | Casing/native/display behavior unobserved | GitHub hosted runner and xvfb | First complete green Ubuntu matrix |
| Renderer-neutral Core boundary | `Game.Core` | `verified` | Core simulation/content/persistence | `Game.Core/**/*.cs`, project graph | assembly/project/source boundary tests plus full suite | No MonoGame/XNA/Client reference found; boundary is automated | unknown | Hosted graph remains unobserved | project graph | Keep boundary gate green as runtime modules expand |
| Local Debug/Release solution | Solution | `verified` | All four projects | `YjsE.sln`, project files | full builds | Post-integration Debug and Release pass with 0 warnings/errors | not a performance metric | Hosted OS evidence separate | exact SDK and locked restore | Same result in hosted matrix |
| Full unit/contract suite | `Game.Tests` | `verified` | Core, client rendering and repository contracts | `Game.Tests/**/*.cs` | 995 tests | 995/995 in post-integration Debug and Release | not a performance metric | Hosted Linux/Windows not observed | solution builds and content | 995/995 across hosted matrix |
| Project/content packaging | `Game.Client`, `Game.Core.Projects` | `verified` | Normal startup and published smoke | `Game.Client.csproj`, `ClientPaths`, project loaders | project/content tests plus Release publish | Final integrated Release publish and generic content-ID scene matrix succeed | exact final payload inventory was not recaptured | No standalone installer or external-game fixture | project manifest and content loader | Add an external game-pack publish fixture |
| Base/mod sprite source provenance | `Game.Core.Data`, `Game.Core.Assets` | `verified` | Runtime sprite resolution | `GameContentLoader.cs`, `SpriteAssetDefinition.cs` | base override and mod-only integration test | Published smoke uses project loader; per-pack absolute roots preserved | included in preload sample | Symlink escapes are not resolved; mod dependency ordering is still basic | content pack loader | Add multi-mod ordering/dependency tests |
| Texture resource/frame ownership | `Game.Client.Rendering` | `verified` | Tile, entity, item, background, effects and UI draw paths | registry/resource/frame files | sharing, fallback, path, disposal and residency tests | Current five-scene smoke: 198 PNGs plus fallback, 199 resources, 1,062 runtime frame descriptors and 0 invalid | 5,453,824 B decoded estimate; Wave 05 contributes 24 assets, 122 explicit source-rect frames and 12 implicit default frames | estimate is not GPU RAM; no atlas | source provenance and graphics device | Add atlas pages without regressing one-resource-per-source |
| Texture preload groups/budgets | `Game.Client.Rendering` | `verified` | UI, World, Entities, Backgrounds and Effects use explicit decoded-byte budgets | `ClientTextureRegistry`, `TextureGroups/**`, `PlayingState.LoadContent` | 13 residency/group/LRU/lease tests plus smokes | Deterministic preload, LRU eviction, pinning, alias-shared leases and resident-only Draw lookup pass | per-group resident bytes and eviction telemetry active | budgets are decoded-RGBA estimates rather than measured GPU memory | texture ownership | Calibrate budgets on a production pack and add atlas pages |
| Client startup/nonblank render | `Game.Client`, Core session/world generation | `verified` | Dedicated smoke mode, normal menus and forced biome scenes | `ClientSmokeOptions`, `MainGame`, `PlayingState`, `GameSessionBootstrapper`, `InfiniteWorldChunkGenerator.GetSurfaceHeightAt` | generic safe `--scene-biome` contract, 6 parser cases and 17 focused world/session cases | Parser accepts normalized data-driven biome IDs; smoke suppresses debug/profiler overlays; forced scenes use deterministic local surface height and a bounded 5x5 collision-chunk preload; new sessions start at day midpoint while loaded saves retain stored time; five current local scenes and visual QA pass | texture sample above | Hosted scene evidence remains separate | final Release publish and DesktopGL | Add encounter-focused visual smokes with active entities and combat effects |
| Client resource lifetime | `Game.Client`, game states | `verified` | Shutdown and state replacement | `MainGame.Dispose`, `GameStateManager.Dispose`, state disposal | registry exact-once tests | Published smoke exits cleanly with state/event/GPU wrapper disposal | unknown | Long-session device reset/reload not covered | MonoGame lifecycle | Add repeated state/load-unload stress smoke |
| Streaming excluded from Draw | `Game.Client.PlayingState` | `verified` | Active sideview client | `PlayingState.Update/Draw` | full suite, source review and 38 streaming tests | Camera planning and bounded apply execute in Update; disk read, generation, decode and dirty saves run in background jobs | operation, elapsed-time and decoded-byte apply budgets plus queue/retry telemetry are active and settings-backed | calibrated main-thread update distribution is still absent | streaming service | Add calibrated long camera-trace distributions |
| Background chunk streaming | `Game.Core.World.Streaming` | `verified` | Infinite-world client | `ChunkStreamingService`, request snapshots, jobs, retry policy, telemetry | focused streaming 38/38 plus full suite | Cancellable load/generate/save jobs, session tokens, stale rejection, classified exponential retry/backoff, terminal suppression/reset, oversize progress, recovery-safe dirty unload and bounded apply pass locally including negative X | calibration initial window avg 7.762 ms, p99 8.235 ms and 2,564,943 B average setup allocation | hosted/Linux behavior and representative long-session distribution remain unobserved | world/session ownership and existing region format | Calibrate cold/warm camera traces and retry incidence |
| Chunk render-cache hot path | `Game.Client.Rendering` | `verified` | Tilemap renderer | `ChunkRenderCache.cs`, `TilemapRenderer.cs` | loaded trim, LRU budget, hit stability | No per-frame `ToHashSet`, `Keys.ToArray` or LINQ trim allocation remains | before/after allocation trace missing | Render batches and texture switches still unknown | active world renderer | Add render-frame allocation and batch counters |
| Authoritative Core simulation host | `Game.Core.Runtime`, `PlayingState` | `verified` | Active sideview client submits buffered commands/item-use requests to its session simulation | `GameSimulation`, `GameFrameSnapshot`, `ImmutableSnapshotList`, `LivingWorldRuntime`, `LoadedGameSession`, `PlayingState` | ordered phases, guard/combat, AI attack/death lifecycle, immutable ownership/enumerator performance, replay capture, architecture and full suite | One fixed-tick host owns living world, player, guard, AI attacks, deterministic deaths/loot, combat, spawning, world simulation, lighting and optional bounded replay capture | calibration avg 0.065 ms, p95 0.098 ms, p99 0.129 ms and 6,237 B average allocation; `FrameSnapshot` contributes 3,132 B/tick in this profile | no previous-frame interpolation; snapshots and entity/query paths still miss ideal 0 B | completed streaming boundary | Reduce frame snapshot and entity-query allocations while preserving read-only handoff and 4 ms/16,384 B gates |
| Core lighting plus 2D presentation lighting | `Game.Core.Lighting`, weather/ambient state, client presentation renderer | `verified` | Fixed simulation resolves visible-first dirty tile light; local-surface living-world state drives cave ambience; Update prepares viewport masks; Draw composites prepared maps | `LightingSystem`, `LightingOptions`, `SkyExposureState`, `LivingWorldRuntime`, `InfiniteWorldChunkGenerator`, `LightingRenderer`, `Rendering/Lighting/**`, `PixelAtmosphereRenderer` | local-surface height, shaft/side-chamber falloff, unknown/load-order invalidation, visible-first scheduling, first-solid attenuation, mined shadow, torch, ray/AO/penumbra/bloom and allocation tests | Sky exposure is explicit as Open/Unknown/Occluded; unloaded dependencies cannot leak daylight or point light, materialization/unload re-dirties dependents, and deterministic visible-first scheduling keeps the camera region responsive while preserving bounded cave falloff | Final BDN dry: 4 chunks 15.62 ms/40 B and 12 chunks 28.50 ms/40 B; previous allocation was 210.53/618.23 KB | RGB simulation light, dynamic entity lights and calibrated GPU traces remain open | tile/biome definitions, streaming lifecycle, settings and prepared resources | Add RGB simulation light, dynamic entity/projectile emitters and 1080p/1440p captures |
| Screen-space reflections and UI backdrop effects | `Game.Client.Rendering.Effects`, `Game.Client.UI` | `verified` | Playing scene capture composites water/wet surfaces and is reused for open-overlay backdrop blur | `ScreenSpaceEffectsRenderer`, `WaterReflectionSurfacePlanner`, `UiTheme`, `PlayingState` | surface planning, quality, pass budgets and UI theme/interaction tests | Reflections and blur are wired to settings and prepared outside authoritative gameplay | capture pixels/surfaces/taps are bounded; GPU timing unknown | screen-space data cannot reflect off-screen geometry; blur is a bounded multi-tap approximation | presentation settings and render targets | Capture visual/performance matrix for disabled/low/medium/high tiers |
| Modern pointer/gamepad UI interaction | `Game.Client.UI`, menu states, settings | `verified` | Main menu, world select/create, pause/settings, HUD and overlays share control/theme contracts | `UiInteraction`, `UiTheme`, `PauseMenuOverlay`, menu states | release-inside, capture, drag, dropdown, segmented, toggle, scroll, tooltip, keyboard/gamepad and settings tests | Mouse-first menus and drag sliders pass local state smokes | no full screenshot regression suite; bitmap font remains current text path | input snapshots and settings persistence | Add automated click-path smokes for create/load/settings/inventory/crafting |
| Fixed-tick layered character animation | `Game.Core.Animation`, content loader, client character/entity renderers | `verified` | Active player consumes the game-owned `player.wave04` rig/state/action profile; active entity preparation resolves the same central runtime registry once at content load | animation content definitions/registry/JSON loader, `GameContentDatabase.RuntimeAnimations`, `Wave04PlayerCharacterRenderer`, `RuntimeEntityVisualProfileCatalog`, `wave04_runtime_profiles.json` | 49 animation tests plus content/mod override, source-rectangle, fallback, steady-allocation and 200-entity preparation suites | Eight clips, five player layers, four action bindings, 14 entity profiles and four typed fallbacks drive player and entity render commands; one PNG resource supplies many source-rect frames and the prepared 200-entity loop is 0 B steady state | command capacities are fixed; render-frame GPU batches remain unknown | encounter screenshots do not yet prove every attack/death/elite state | sprite manifest and fixed simulation | Add encounter captures and authored attack/death event timelines |
| Guard, parry, attacks and advanced projectiles | `Game.Core.Combat`, `Game.Core.Projectiles`, `GameSimulation` | `verified` | Secondary input drives authoritative guard; one damage query resolves projectiles plus contact | guard/attack/damage contracts, `AttackSequencer`, command/event buffers, sequence compiler, projectile runtime/factory | 88 focused attack tests plus parry, guard break, mitigation, friendly fire, exact-once, lifetime, homing, pierce and bounce | Tick-native startup/active/recovery/cooldown, buffering, cancel/combo windows, resource metadata, multiple swept shapes and bounded hit tracking are reusable and 0 B steady state | gameplay selected-item paths still retain their legacy cooldown/execution authority; balancing distribution unknown | entity queries, RNG, events and item use | Make every authored melee/ranged/magic item consume the sequencer |
| Audio mixer and living soundscapes | `Game.Core.Audio`, `Game.Client.Audio` | `partial` | Playing snapshot selects ambient soundscape commands and applies persisted category volumes | mixer/envelopes/soundscape contracts and loaders, client registry/manager/controller | mixer, planner and client audio tests | Runtime adapter updates and exposes voice/missing/transition telemetry | no shipped sound files; missing assets fall back without crash | concrete music/SFX asset pack and device variance remain absent | living-world snapshot and settings | Add licensed/generated audio pack, event SFX routing and device-loss smoke |
| Variable render frame rate | `Game.Core.Settings`, `MainGame`, pause/settings UI | `implemented-unverified` | Active client applies unlimited or 30..360 FPS while simulation remains fixed at 60 Hz | `GameSettings.cs`, `GameSettingsValidator.cs`, `MainGame.cs`, `PauseMenuOverlay.cs` | settings validation/round-trip and fixed-update contracts | Debug/Release builds and client smokes pass | no frame-pacing distribution captured | no exclusive fullscreen refresh-rate enumeration | MonoGame timing and video settings | Capture 30/60/120/unlimited frame pacing without simulation divergence |
| Friendly/hostile entity ecosystem | `Game.Core.Entities`, AI, spawning and loot | `verified` | Activity-source spawn rings and faction actors consume authoritative player/living-world state | `Entities/AI/**`, `Spawning/**`, `EntityAttackSystem`, `EntityDeathLifecycle`, `Loot/**` | distribution, caps, habitat, memory, flock/flee/chase/return-home, deterministic death and 200-entity soaks | Two-source spawning, negative/positive X, protected despawn, telemetry and exactly-once loot pass; Wave 05 adds marsh frog, canopy owl, amber beetle and prism wisp actors with loot/spawn contracts | pre-Wave05 200-entity spawn dry smoke 3.646 ms/64 B; current AI allocation test stays within 12 KiB/tick | final-tree 200-entity calibrated distribution and Wave 05 encounter screenshot are absent; local steering is not a global navigation graph | living-world snapshot and named RNG | Calibrate final-tree 200-entity CPU distributions and expose AI telemetry in debug UI |
| Regional world/weather/event contracts | Core generation, biomes, weather and world events | `verified` | `LivingWorldRuntime` owns the advanced deterministic executor, advances scheduled events at most once per 60 ticks and evaluates successful player actions exactly once | `LivingWorldRuntime`, `WorldEvents/**`, `WorldEventStateSaveService`, `GameContentDatabase.WorldEvents`, regional planner/profile/registry, infinite generator | runtime throttle/phase/journal, sidecar recovery/migration/resume, exact-once scheduled/action activation, rare-loot, replay and feedback suites | Active event, cooldowns, bounded journal and last action sequence persist atomically; scheduled/action activation, modifiers and normal/rare drops route exactly once into bounded visual/audio cues | planner trace covers 4,097 positions; current simple-generation quick avg 27.886 ms | trigger/room zones and production audio remain open | content loader, world time, named RNG, saving and GameSimulation | Add dynamic trigger/room zones and authored event encounters |
| Developer console and typed commands | Core commands/developer tools plus client overlay | `verified` | Playing adapter exposes execution, completion selection, separate history/output navigation, scrolling and help | command/developer-tool files, console adapter/model/overlay | 23 client/UI tests plus command suites | Published Playing smoke opens and renders the console at 640x360 | stable-model tests cover key UI paths | some intents need richer domain-specific output | command context and session | Add scripted execution smoke for every mutating command family |
| Download and engine-doc website | `Website/**` | `verified` | Static game/download page is separated from the 25-article searchable engine wiki | HTML/CSS/JS, JSON data, update and validation tools | JSON/JS/local-link/PNG/provenance validators | Static gate passes: 2 HTML, 25 docs, 52 local references, 41 PNGs and 30 byte-exact Game.Data copies; desktop/mobile browser QA, deep links, search and navigation pass | dependency-free static site | no hosted URL or downloadable artifact; website data is a presentation copy and not an engine source of truth | validated release artifacts and ledger | Publish the first signed artifact and activate its honest download entry |
| Deterministic runtime RNG/replay | Core runtime/gameplay | `verified` | Session-owned named streams drive combat, status, spawn, farming, event actions and death keys; optional simulation capture records authoritative inputs | `Randomness/**`, `Diagnostics/Replay/**`, `random-state.json`, `world-events.json`, simulation/save/load/bootstrap, state hasher | 20 replay-contract tests plus isolation, recovery, mid-trace resume and dual-session replay | Versioned bounded input frames include delta, player/item input and checkpoint hashes; JSON has a 64 MiB cap and first-divergence reports order/input/checkpoint/hash/version plus last matching checkpoint | calibration state hash avg 0.974 ms; 1,200-tick dual replay avg 239.922 ms | replay recording is an explicit diagnostic session and is not yet persisted as a save sidecar | authoritative simulation and additive save sidecars | Add playback orchestration, user-facing export and save-linked replay metadata |
| Core fixed-tick allocation budget | `Game.Core.Runtime` | `partial` | Representative session with living world, guard/combat and active AI | benchmark, phase allocation export, immutable snapshot ownership and flag hotpaths | calibration, determinism, snapshot ownership/enumerator and phase allocation gates | current quick thresholds pass; direct immutable enumeration, attack sequencing, feedback draining and tile flag queries are 0 B | calibration avg 0.065 ms/p95 0.098 ms/p99 0.129 ms; 6,237 B average allocation | `FrameSnapshot` remains 3,132 B/tick in calibration and entity/query churn remains | authoritative simulation host | Introduce bounded snapshot buffer ownership and reduce entity/query churn without mutable leakage |
| Content loading/cross-reference validation | `Game.Core.Data` | `verified` | Project bootstrap, benchmark and smoke | loaders, registries, validator and `AnimationContentJsonLoader` | post-integration Debug/Release content and full suite | Central loading includes runtime clips, rigs, state machines, character actions, entity visual profiles and player-action event triggers with deterministic base/mod overrides and diagnostics alongside all existing content | current quick load avg 56.547 ms and 2,778,368 B average | representative multi-mod set and external JSON schema gate are missing | project/content paths | Add deterministic representative mod-pack benchmark/validation and schema diagnostics |
| Sprite manifest/brief/audit contract | `Game.Data`, `Game.Core.Assets` | `partial` | CI/tooling and client preload | manifests, briefs, Python audit v2, C# asset tests | asset/content suites plus strict Python gate | Main CI scope passes at 180 IDs/174 PNGs/6 aliases/0 hard issues; supplemental Wave 05 report reaches 204 definitions/0 hard issues | main scope has 146 pass and 34 review; Wave 05 adds 24 contract-complete definitions | Wave 05 lives under `assets/Wave05`, so 24 paths appear outside the audit's `sprites/**` disk inventory even though existence/dimensions/provenance and runtime preload pass | style contract and Pillow | Normalize Wave 05 into the canonical sprite scope or teach the strict audit to scan all manifest roots |
| Representative production asset waves | layered player, biome backgrounds, ambience/weather/combat effects and UI | `partial` | Wave 04 player/background/ambient paths are active; Wave 05 content is loaded and pre-resident in the final smokes | Wave 04/05 PNGs, generators, manifests, briefs, prompts, provenance and previews | Wave 04 31/31 contracts, Wave 05 24-definition/122-explicit-frame supplemental report and post-integration content tests | Wave 05 adds two seamless backgrounds, four animated actors, six items/tools, four autotiles, two objects and six UI assets; final Amber Grove and Twilight Marsh captures confirm their biome presentation | 5,453,824 B decoded estimate for the 199-resource/1,062-runtime-frame checkpoint preload | visual proof of every item/entity/effect binding is still absent | texture groups, content registries and animation profiles | Add encounter/item/effect capture matrices and normalize the Wave 05 audit root |
| Mining/building/interaction transactions | `Game.Core.Interaction`, `GameSimulation`, client feedback | `verified` | `BuildingSystem` delegates active placement to `BuildingPlacementTransactionService`; selected item use and targeting resolve typed candidate/validation/commit results | query/hold/mining tuning, candidate factory, building validator/system/transaction service | active building, atomic inventory/world, negative-X, mining and feedback suites | Faster global mining tuning, pass-through mineable `mangrove_root`, collision/support/reach checks and optimistic/authoritative inventory transactions pass | no calibrated interaction CPU trace | furniture anchors, background walls, liquids, rollback IDs and server request validation remain open | world edits, inventory and item definitions | Add versioned authoritative interaction request/rollback IDs, then furniture/wall/liquid placement rules |
| Inventory/crafting transactions | `Game.Core.Inventory`, `Game.Core.Crafting`, client overlays | `verified` | Active sideview inventory and crafting UI | transaction/query/organization/batch/search/tracking services | focused inventory/crafting/save contracts plus full suite | Atomic batch craft, max craft, favorites, cursor return, equipment preflight and typed result events pass locally | recipe refresh now revision-cached; no UI trace baseline | Recipe unlock/pin session persistence and overlay interaction automation remain incomplete | item definitions, save service and UI adapter | Persist knowledge/pins and add overlay input tests |
| Save/load round trips | `Game.Core.Saving` | `verified` | Session/world persistence | saving/session services, `SimulationSaveService`, `RandomStateSaveService`, `WorldEventStateSaveService` | save/migration/hash/RNG/event recovery tests | Negative X, player v1-v3, simulation time, RNG and world-event sidecars round-trip; event state uses atomic replace, backup recovery, future-version rejection, legacy defaults and registry normalization for removed mods | save time/bytes unknown | region overwrite recovery, save rotation and user-facing corruption reports remain incomplete | content registries and filesystem | Add interrupted region-write recovery, integrity reports and save rotation |
| Benchmark executable | `Game.Benchmarks` | `verified` | CI quick smoke, local calibration and BenchmarkDotNet categories | stable harness, phase allocation export and `EngineBenchmarks.cs` | executable quick/calibration contracts, immutable query and lighting BDN dry runs | Current quick fixed tick is 0.099 ms average/0.131 ms p95 and 7,250 B average; calibration is 0.065 ms/6,237 B; typed events remain 0 B and replay matches | lighting BDN allocation remains 40 B for both 4/12 chunks; current content load calibration is 49.065 ms and 2,780,534 B average | BDN dry is one operation and not a calibrated distribution; no renderer GPU benchmark or automatic baseline-comparison gate yet | stable fixtures | Add calibrated BDN short runs and threshold comparison in CI |
| Performance budgets | Engineering control plane | `partial` | Acceptance decisions | `PERFORMANCE_BUDGETS.md`, benchmark/smoke artifacts | evidence review | Phase, cold/warm streaming and 200-entity smoke evidence added; unknown fields retained | see budget document | save, GPU render and calibrated entity/streaming distributions remain unknown | telemetry and representative fixtures | Export phase attribution and calibrated GPU/streaming/entity distributions |
| Durable resume contract | Repository control plane | `verified` | Every future `Weiter` session | `AGENTS.md`, `WORK_LEDGER.md` | document consistency review | Ledger names one exact next action and validation set | not a performance metric | Requires disciplined updates after every session | source-of-truth order | Resume at Epic 2 Milestone 4 without replanning |
