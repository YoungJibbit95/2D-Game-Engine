# YjsE Capability Matrix

Last updated: 2026-07-19 after the cutoff-free depth-stack, Tree V2, Wave 06 character and status-dock pass

Capability status values are limited to `verified`, `implemented-unverified`, `partial`, `planned`, `blocked`, and `deprecated`. Local evidence is never presented as a hosted Windows/Ubuntu result.

## Validation Baseline

- Audit-start HEAD: `f9a06072a1172d2bd8d064cabad92235443dd8c8` on `master`, with a pre-existing dirty tree.
- Audit-start Debug and Release: builds passed; tests were 459/461 in each configuration.
- Current Debug and Release solution builds pass with 0 warnings/errors and both full suites pass 1381/1381. The integrated visual-polish scope passes 158/158 in both configurations; focused Parallax passes 80/80. Exact allocation and timing micro-gates run serially so their evidence is not contaminated by unrelated test work.
- Focused gates cover responsive UI layout, layered animation, bounded procedural backgrounds/particles, guard/projectiles, activity-source spawning, 500-entity AI/spawn scale, structures, 2D ray-cast lighting/reflections, audio, living world, RNG/save resume, texture residency and content separation.
- Strict Python main-manifest audit: 190 IDs, 184 canonical PNG sources plus 6 valid source-alias groups and 0 hard issues. The supplemental Wave 05 report covers 206 definitions and 0 hard issues, but records 24 Wave 05 paths outside the CI audit's `sprites/**` disk-scope accounting. Soft review debt remains 104 palette, 32 silhouette and 2 occupancy findings.
- Current V5 four-biome smokes remain green; the final V6 Forest visual-polish smoke also passes at 2560x1440 with 235 resources, 1,308 frames and 0 invalid resources. Visual inspection verifies organic Far silhouettes, stable multi-depth density and removal of the former rectangular PNG edge; hosted Windows and Ubuntu remain `implemented-unverified`.

## Matrix

| Capability | Owner project/module | Status | Active runtime usage | Implementation files | Tests | Runtime validation | Performance baseline | Known limitations | Dependencies | Next acceptance criterion |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Exact .NET SDK selection | Repository control plane | `verified` | All local .NET commands | `global.json` | Build/restore gates | Current host selects exactly 8.0.420 with roll-forward disabled | not a performance metric | Hosted selection not observed | SDK distribution/setup action | Windows and Ubuntu CI both report 8.0.420 |
| Locked NuGet restore | Repository control plane | `verified` | All four projects | `Directory.Build.props`, four `packages.lock.json` files | locked restore | Local `--locked-mode` passed | not a performance metric | Hosted feeds/platform graphs unobserved | NuGet sources and exact SDK | Hosted locked restore passes on both OS families |
| Compiler/analyzer/style policy | Repository control plane | `verified` | All C# projects | `Directory.Build.props`, `.editorconfig` | style verify and builds | C# 12, .NET 8 analyzers and warnings-as-errors pass locally | not a performance metric | Gate intentionally verifies C# style, not repository-wide line-ending normalization | locked restore | Hosted style/build matrix passes without suppressions |
| Windows CI | GitHub Actions | `implemented-unverified` | PR, push and manual workflows | `.github/workflows/ci.yml` | Debug/Release focused/full gates | Workflow implemented, no hosted run | unknown | Requires a committed/pushed revision | GitHub hosted runner | First complete green Windows matrix |
| Linux CI | GitHub Actions | `implemented-unverified` | PR, push and manual workflows | `.github/workflows/ci.yml` | Debug/Release plus xvfb smoke | Workflow implemented, no hosted run | unknown | Casing/native/display behavior unobserved | GitHub hosted runner and xvfb | First complete green Ubuntu matrix |
| Renderer-neutral Core boundary | `Game.Core` | `verified` | Core simulation/content/persistence | `Game.Core/**/*.cs`, project graph | assembly/project/source boundary tests plus full suite | No MonoGame/XNA/Client reference found; boundary is automated | unknown | Hosted graph remains unobserved | project graph | Keep boundary gate green as runtime modules expand |
| Local Debug/Release solution | Solution | `verified` | All four projects | `YjsE.sln`, project files | full builds | Post-integration Debug and Release pass with 0 warnings/errors | not a performance metric | Hosted OS evidence separate | exact SDK and locked restore | Same result in hosted matrix |
| Full unit/contract suite | `Game.Tests` | `verified` | Core, client rendering and repository contracts | `Game.Tests/**/*.cs` | focused and full-suite runs | Current Debug and Release both pass 1381/1381 after renderer, parallax, streaming, snapshot, movement/physics, active-liquid, combat-query, worldgen-workspace and visual-polish integration | not a performance metric | Hosted Linux/Windows not observed | solution builds and content | Reproduce 1381/1381 across the hosted matrix |
| Project/content packaging | `Game.Client`, `Game.Core.Projects` | `verified` | Normal startup and published smoke | `Game.Client.csproj`, `ClientPaths`, project loaders | project/content tests plus Release publish | Final integrated Release publish and generic content-ID scene matrix succeed | exact final payload inventory was not recaptured | No standalone installer or external-game fixture | project manifest and content loader | Add an external game-pack publish fixture |
| Base/mod sprite source provenance | `Game.Core.Data`, `Game.Core.Assets` | `verified` | Runtime sprite resolution | `GameContentLoader.cs`, `SpriteAssetDefinition.cs` | base override and mod-only integration test | Published smoke uses project loader; per-pack absolute roots preserved | included in preload sample | Symlink escapes are not resolved; mod dependency ordering is still basic | content pack loader | Add multi-mod ordering/dependency tests |
| Texture resource/frame ownership | `Game.Client.Rendering` | `verified` | Tile, entity, item, background, effects and UI draw paths; tile frames use CPU-baked padded atlas pages | registry/resource/frame files, `TextureAtlasPlanner`, `TilemapRenderer` | sharing, fallback, path, disposal, residency and atlas planning tests | Current final smoke loads 234 PNGs plus fallback, 235 resources, 1,308 runtime frame descriptors and 0 invalid; tile atlas construction occurs during content configuration, never Draw | 34,744,320 B decoded source estimate plus explicit atlas-page byte telemetry | estimate is not measured GPU RAM; atlas currently covers tile frames only | source provenance and graphics device | Extend compatible packing to entity/UI categories and calibrate GPU residency |
| Texture preload groups/budgets | `Game.Client.Rendering` | `verified` | UI, World, Entities, Backgrounds and Effects use explicit decoded-byte budgets | `ClientTextureRegistry`, `TextureGroups/**`, `PlayingState.LoadContent` | 13 residency/group/LRU/lease tests plus smokes | Deterministic preload, LRU eviction, pinning, alias-shared leases and resident-only Draw lookup pass | per-group resident bytes and eviction telemetry active | budgets are decoded-RGBA estimates rather than measured GPU memory | texture ownership | Calibrate budgets on a production pack and add atlas pages |
| Client startup/nonblank render | `Game.Client`, Core session/world generation | `verified` | Dedicated smoke mode, normal menus and forced biome scenes | `ClientSmokeOptions`, `MainGame`, `PlayingState`, `GameSessionBootstrapper`, `InfiniteWorldChunkGenerator.GetSurfaceHeightAt` | generic safe `--scene-biome` contract, 6 parser cases and 17 focused world/session cases | Parser accepts normalized data-driven biome IDs; smoke suppresses debug/profiler overlays; forced scenes use deterministic local surface height and a bounded 5x5 collision-chunk preload; new sessions start at day midpoint while loaded saves retain stored time; five current local scenes and visual QA pass | texture sample above | Hosted scene evidence remains separate | final Release publish and DesktopGL | Add encounter-focused visual smokes with active entities and combat effects |
| Client resource lifetime | `Game.Client`, game states | `verified` | Shutdown and state replacement | `MainGame.Dispose`, `GameStateManager.Dispose`, state disposal | registry exact-once tests | Published smoke exits cleanly with state/event/GPU wrapper disposal | unknown | Long-session device reset/reload not covered | MonoGame lifecycle | Add repeated state/load-unload stress smoke |
| Streaming and chunk rebuilding excluded from Draw | `Game.Client.PlayingState`, tile renderer | `verified` | Active sideview client | `PlayingState.Update/LateUpdate/Draw`, `TilemapRenderer`, `ChunkRenderCache` | full suite, cache behavior and 38 streaming tests | Camera planning and bounded apply execute in Update; visible dirty chunk commands rebuild under a bounded LateUpdate budget; Draw only consumes prepared cache entries | current V3 traversal averages 0.003 ms chunk preparation; streaming operation/time/byte budgets remain active | calibrated long-session rebuild distribution is still absent | streaming service and chunk dirty flags | Add calibrated long camera/rebuild distributions |
| Background chunk streaming | `Game.Core.World.Streaming` | `verified` | Infinite-world client | `ChunkStreamingService`, compact window request snapshots, jobs, retry policy, telemetry and bidirectional harness | focused streaming contracts, planner/allocation gates and full suite | Cancellable jobs, session tokens, retry/backoff, recovery-safe dirty unload and bounded apply remain active; rectangular required/retain sets avoid per-camera hash tables and Debug workers receive fair settlement time | unchanged-camera plan 664 B; calibration initial window 9.896 ms/1,796,413 B; two 65-position cold p99 runs 99.3-203.3 ms and warm p99 0.014-0.015 ms | hosted/Linux behavior and region-read/generate/apply/save attribution remain unobserved | world/session ownership and existing region format | Add labelled service-phase distributions without regressing compact planning |
| Chunk render-cache hot path | `Game.Client.Rendering` | `verified` | Tilemap renderer | `ChunkRenderCache.cs`, `TilemapRenderer.cs`, atlas planner/telemetry and renderer metrics telemetry | loaded trim, LRU budget, hit stability, atlas packing and renderer counter tests | No per-frame `ToHashSet`, `Keys.ToArray`, LINQ trim allocation or texture readback remains; tile frames resolve into atlas-page source rectangles and MonoGame submission deltas are exported | final 1440p Forest averages 55.53 Draw submissions/binding changes and 0.296 ms Tilemap Draw at 0 B with the expanded 1,308-frame content set | GPU time is unsupported by MonoGame metrics; chunk geometry is still SpriteBatch command data rather than persistent vertex/index buffers | active world renderer | Add render-target/geometry-backed chunk batches and compare long traversal distributions |
| Authoritative Core simulation host | `Game.Core.Runtime`, `PlayingState` | `verified` | Active sideview client submits buffered commands/item-use requests to its session simulation | `GameSimulation`, `GameFrameSnapshot`, `EntityFrameSnapshotBuilder`, `ImmutableSnapshotList`, `LivingWorldRuntime`, `LoadedGameSession`, `PlayingState` | ordered phases, immutable history, AI-field preservation, 0/500-entity distributions, replay and full suite | One fixed-tick host owns authoritative state. Inventory/farm caches, columnar copy-on-change snapshots and caller-owned pickup/combat query workspaces preserve immutable historical frames including faction/AI target/telemetry | latest quick 0.116 ms average/0.261 ms p99 and 3,125 B average allocation; calibration 0.102 ms/0.257 ms p99 and 2,690 B; 500 moving entities snapshot at 5,000.2 B/tick | no previous-frame interpolation; remaining snapshot columns miss ideal 0 B | completed streaming boundary | Reduce remaining snapshot columns and design interpolation without mutable leakage |
| Core lighting plus 2D presentation lighting | `Game.Core.Lighting`, weather/ambient state, client presentation renderer | `verified` | Fixed simulation resolves at most one visible-first dirty chunk per tick by default; local-surface living-world state drives separate atmosphere/cave ambience; one LateUpdate prepares viewport masks; Draw only composites prepared maps | `LightingSystem`, `LightingOptions`, `SkyExposureState`, `LivingWorldRuntime`, `InfiniteWorldChunkGenerator`, `LightingRenderer`, `Rendering/Lighting/**`, `PixelAtmosphereRenderer` | local-surface height, daylight without buried bleaching, bounded ray distance, fractional point-light visibility, 1/3-sample tiering, AO/penumbra/bloom, reflection-radiance and allocation/performance tests | Medium uses one bounded point-shadow ray; High uses three endpoint-spread rays for fractional penumbra edges. Colored light plus daylight feeds a bounded reflection-radiance map, and shadow/color/bloom/reflection textures use content-hash-checked triple buffering | Focused lighting gates pass 22/22 Debug/Release with 0 B hot-path assertions. Final V5 traversal averages 0.535 ms mask/0.370 ms upload at 1080p and 0.601/0.210 ms at 1440p; preparation averages 1.004/0.955 ms | actual GPU timestamps, RGB authoritative simulation light and dynamic entity/projectile emitters remain open; rare upload/OS peaks remain visible | tile/biome definitions, streaming lifecycle, settings and prepared resources | Attribute upload tails by content change, then add dynamic entity/projectile emitters without regressing the current mask budget |
| Screen-space reflections and UI backdrop effects | `Game.Client.Rendering.Effects`, `Game.Client.UI` | `verified` | Playing scene capture composites water/wet surfaces and feeds a quality-scaled prepared Kawase backdrop; lighting contributes a low-resolution colored radiance layer | `ScreenSpaceEffectsRenderer`, `BackdropBlurPlanner`, `WaterReflectionSurfacePlanner`, `ReflectionRadianceMapBuilder`, `PresentationWorkScheduler`, `PresentationFrameBudget`, `UiTheme`, `PlayingState` | surface/radiance/blur planning, quality, pass budgets, cadence, frame-budget, UI theme and interaction tests | Lighting, reflections, atmosphere and capture use independent bounded rates; overlay blur downsamples once per captured scene and reuses the prepared texture | planner hot paths assert 0 B; High 1080p radius-8 blur reduces estimated samples from 16.59M to 6.22M (62.5%); open-pause smoke passes at 6.098 ms average and 1.398 ms CPU Draw | screen-space data cannot reflect off-screen geometry; lighting and scene capture still scan the same surface plan separately; GPU timing unknown | presentation settings and render targets | Share the surface plan, map graph alias slots to a transient target pool and capture the 1440p tier matrix |
| Modern pointer/gamepad UI interaction | `Game.Client.UI`, menu states, settings | `verified` | Main menu, world select/create, pause/settings, responsive HUD and overlays share control/theme contracts | `UiInteraction`, `UiTheme`, `PixelUiPrimitives`, `AdaptiveOverlayLayouts`, `GameplayFeedbackPresentation`, `GameplayFeedbackOverlay`, menu states | 79/79 Client UI/UI tests pass in Debug and Release, including responsive containment, deterministic status priority and allocation gates | The gameplay overlay now uses a responsive glass status dock with debuffs first, pixel runes and duration strips; layouts remain contained from 320x240 through 4K | status selection/layout remains 0 B across 10,000 steady-state plans | no automated screenshot-diff suite; bitmap font remains current text path; rename/delete and async save browsing remain open | input snapshots and settings persistence | Add screenshot-regression smokes and migrate remaining inventory/crafting surfaces to the same dock/material language |
| Fixed-tick layered character animation | `Game.Core.Animation`, content loader, client character/entity renderers | `verified` | Active player consumes the game-owned `player.wave06` rig/state/action profile; active entity preparation resolves the same central runtime registry once at content load | animation content definitions/registry/JSON loader, `GameContentDatabase.RuntimeAnimations`, `Wave04PlayerCharacterRenderer`, `RuntimeEntityVisualProfileCatalog`, `wave06_character_runtime_profiles.json` | animation/content/renderer subsets plus full Debug/Release suites | Five synchronized native 24x40 layers expose 16 source-rectangle poses without legacy upscaling; body, clothes, hair, armor and equipment stay on one fixed-tick animation clock | command capacities are fixed; render-frame GPU batches remain unknown | renderer owner retains its legacy Wave04 class name and encounter screenshots do not yet prove every action state | sprite manifest and fixed simulation | Rename the compatibility renderer owner and add encounter captures for all Wave 06 actions |
| Scalable procedural backgrounds and particles | `Game.Client.Rendering` | `verified` | Active Playing background, authored far/mid/near depth planes and feedback/ambient particle paths | `ParallaxBackgroundRenderer`, `ParallaxLayerPlanner`, `ParallaxViewportLayoutPlanner`, `GameplayParticleSystem`, `BackgroundDepthV6/**` | 80/80 Parallax tests pass in Debug and Release across 720p-2160p, ultrawide, jump/zoom invariance, negative X, authored density/repeats and 0 B hot paths | Twelve independent binary-alpha Far/Mid/Near planes retain fixed authored density, distinct 0.08/0.18/0.32 movement ratios and organic sky silhouettes; active Far planes need one Draw per repeat and the background batch removes one redundant End/Begin boundary | final 2560x1440 Forest loads 235 resources/1,308 frames/0 invalid; Background Draw averages 0.063 ms at 0 B | no GPU particle simulation; active-biome residency and complete four-biome mining captures remain open | living-world snapshot, camera, surface resolver and quality settings | Capture the four-biome mining matrix, then add active-biome residency and GPU-driven particle expansion |
| Guard, parry, attacks and advanced projectiles | `Game.Core.Combat`, `Game.Core.Projectiles`, `GameSimulation` | `verified` | Secondary input drives authoritative guard; selected melee/ranged/magic items consume authored attack sequences; guard and launch outcomes enter the bounded feedback queues | guard/attack/damage contracts, `AttackSequencer`, `PlayerAttackSequenceRuntime`, `GameplayFeedbackRouter`, attack registry/JSON loader, command/event buffers, projectile runtime/factory | authored item integration plus 22/22 focused combat-feedback tests covering parry, guard break, exact-once delivery, friendly fire, lifetime, homing, pierce, bounce and 0 B typed adapters | Accepted starts atomically spend resources; authoritative phases materialize timed shapes/projectiles. Guard block/parry/break and projectile launch emit bounded visual/audio cues exactly once | balancing distributions and production audio remain open; bounce/pierce/expire/destroy adapters are tested but not yet attached to every runtime terminal path | entity queries, RNG, events and item use | Wire projectile terminal adapters, capture the combat matrix and author more weapon sequences |
| Audio mixer and living soundscapes | `Game.Core.Audio`, `Game.Client.Audio` | `partial` | Playing snapshot selects ambient soundscape commands and applies persisted category volumes | mixer/envelopes/soundscape contracts and loaders, client registry/manager/controller | mixer, planner and client audio tests | Runtime adapter updates and exposes voice/missing/transition telemetry | no shipped sound files; missing assets fall back without crash | concrete music/SFX asset pack and device variance remain absent | living-world snapshot and settings | Add licensed/generated audio pack, event SFX routing and device-loss smoke |
| Variable render frame rate | `Game.Core.Settings`, `MainGame`, pause/settings UI | `verified` | Active client applies unlimited or 30..360 FPS while simulation remains fixed at 60 Hz; low-latency mode owns VSync policy, MMCSS scheduling and high-resolution waitable-timer pacing; presentation passes have independent cadence and frame-admission budgets | settings/validator, `FramePacingPolicy`, `HighResolutionFrameLimiter`, `GameThreadSchedulingScope`, telemetry/scheduler, `PresentationFrameBudget` and pause settings | settings round-trip, fixed-update, policy, limiter, telemetry, cadence, admission and performance contracts | Final V5 Forest at a 165 cap averages 6.061 ms at 1080p and 6.078 ms at 1440p. Retained windows meet 120 Hz at 99.80%/98.83% and 144 Hz at 99.22%/97.27%; CPU Draw averages 0.609/0.745 ms | frame intervals are limiter/OS observations rather than GPU timestamps; no exclusive refresh enumeration or complete quality/biome matrix; every-frame 165 Hz is not claimed | MonoGame timing, Windows scheduler and video settings | Add refresh enumeration, backend timestamps and disabled/low/medium/high biome traces |
| Friendly/hostile entity ecosystem | `Game.Core.Entities`, AI, spawning and loot | `verified` | Offscreen activity-source ingress and faction actors consume authoritative player/living-world state | `Entities/AI/**`, `Spawning/**`, `EntityAttackSystem`, `EntityDeathLifecycle`, `Loot/**` | loaded-chunk/solid placement, encounter fallback, 90-second integration, repeated 18,000-tick streaming soak and 500-entity gates | Incremental spatial membership, O(1) IDs, bounded AI neighborhoods and deterministic warm-start ingress remove full-list sensing and empty-world startup. Invalid single-member flocking is rejected and zero-neighbor steering fails closed | current isolated Release: 500-entity AI/movement 4.488 ms p99 and 0.5 B/tick; spawn-cap maintenance 0.184 ms p99 and 0 B/tick; repeated soak hash `0xB732F6A63493D950`, 134 spawned/99 despawned, cap 32 | full biome encounter matrix and a global navigation graph remain absent; runtime encounter attribution is not a save-format contract | living-world snapshot, named RNG and loaded chunk residency | Extend visible encounter captures while retaining 500-entity and 18,000-tick gates |
| Regional world/weather/event contracts | Core generation, biomes, weather and world events | `verified` | `LivingWorldRuntime` owns the advanced deterministic executor, advances scheduled events at most once per 60 ticks and evaluates successful player actions exactly once | `LivingWorldRuntime`, `WorldEvents/**`, `WorldEventStateSaveService`, `GameContentDatabase.WorldEvents`, regional planner/profile/registry, infinite generator | runtime throttle/phase/journal, sidecar recovery/migration/resume, exact-once scheduled/action activation, rare-loot, replay and feedback suites | Active event, cooldowns, bounded journal and last action sequence persist atomically; scheduled/action activation, modifiers and normal/rare drops route exactly once into bounded visual/audio cues | planner trace covers 4,097 positions; current 256x128 simple-generation quick avg 0.694 ms | trigger/room zones and production audio remain open | content loader, world time, named RNG, saving and GameSimulation | Add dynamic trigger/room zones and authored event encounters |
| Developer console and typed commands | Core commands/developer tools plus client overlay | `verified` | Playing adapter exposes execution, completion selection, separate history/output navigation, scrolling and help | command/developer-tool files, console adapter/model/overlay | 23 client/UI tests plus command suites | Published Playing smoke opens and renders the console at 640x360 | stable-model tests cover key UI paths | some intents need richer domain-specific output | command context and session | Add scripted execution smoke for every mutating command family |
| Download and engine-doc website | `Website/**` | `verified` | Static game/download page is separated from the 26-article searchable engine wiki | HTML/CSS/JS, JSON data, update and validation tools | JSON/JS/local-link/PNG/provenance validators | Static gate passes: 2 HTML, 26 docs, 52 local references, 41 PNGs and 30 byte-exact Game.Data copies; desktop/mobile browser QA, deep links, search and navigation pass | dependency-free static site | no hosted URL or downloadable artifact; website data is a presentation copy and not an engine source of truth | validated release artifacts and ledger | Publish the first signed artifact and activate its honest download entry |
| Deterministic runtime RNG/replay | Core runtime/gameplay | `verified` | Session-owned named streams drive combat, status, spawn, farming, event actions and death keys; optional simulation capture records authoritative inputs | `Randomness/**`, `Diagnostics/Replay/**`, `random-state.json`, `world-events.json`, simulation/save/load/bootstrap, state hasher | 20 replay-contract tests plus isolation, recovery, mid-trace resume and dual-session replay | Versioned bounded input frames include delta, player/item input and checkpoint hashes; JSON has a 64 MiB cap and first-divergence reports order/input/checkpoint/hash/version plus last matching checkpoint | calibration state hash avg 0.974 ms; 1,200-tick dual replay avg 239.922 ms | replay recording is an explicit diagnostic session and is not yet persisted as a save sidecar | authoritative simulation and additive save sidecars | Add playback orchestration, user-facing export and save-linked replay metadata |
| Core fixed-tick allocation budget | `Game.Core.Runtime` | `partial` | Representative session with living world, guard/combat and active AI | benchmark, phase allocation export, immutable snapshot ownership, columnar entity builder and query buffers | calibration, determinism, immutable historical frames, detailed AI fields, 0/500-entity and full-suite gates | quick thresholds pass; pickup magnetism, combat/projectile queries, representative entity update, immutable enumeration, attack sequencing, feedback draining, flags and spawn-cap maintenance are 0 B steady state | latest quick 0.116 ms/3,125 B; calibration 0.102 ms/2,690 B | `FrameSnapshot` remains 2,419 B quick/1,621 B calibration; interpolation remains open | authoritative simulation host | Reduce remaining snapshot columns and add previous-frame interpolation |
| Content loading/cross-reference validation | `Game.Core.Data` | `verified` | Project bootstrap, benchmark and smoke | loaders, registries, validator and `AnimationContentJsonLoader` | post-integration Debug/Release content and full suite | Central loading includes runtime clips, rigs, state machines, character actions, entity visual profiles and player-action event triggers with deterministic base/mod overrides and diagnostics alongside all existing content | current quick load avg 56.547 ms and 2,778,368 B average | representative multi-mod set and external JSON schema gate are missing | project/content paths | Add deterministic representative mod-pack benchmark/validation and schema diagnostics |
| Sprite manifest/brief/audit contract | `Game.Data`, `Game.Core.Assets` | `partial` | CI/tooling and client preload | manifests, briefs, Python audit v2, C# asset tests | asset/content suites plus strict Python gate | Main scope passes at 190 manifest entries/184 PNGs/0 hard issues; all four V5 panoramas are seam/dimension/brief/provenance complete | V5 uses native 1536x384 RGBA sources, a 256-color ceiling and zero horizontal seam delta | Wave 05 remains outside part of the canonical `sprites/**` review inventory and soft cross-wave style debt remains | style contract and Pillow | Normalize Wave 05 and add a cross-wave style/scale audit rather than only file-contract validation |
| Representative production asset waves | layered player, biome backgrounds, ambience/weather/combat effects and UI | `partial` | Wave 06 player, Wave 05 content, V5 source panoramas, V6 depth planes and Tree V2 foreground assets are active | Wave 05/06/V5/V6 PNGs, generators, manifests, briefs, prompts, provenance, audits and previews | strict V6 12/12, Wave 06 5/5 and foliage 3/3 audits plus 1440p runtime smoke | The active player is native 24x40, four biomes bind independent depth stacks, and biome tree silhouettes/materials no longer rely on one rectangular green canopy | current smoke estimates 34,744,320 B decoded across 235 resources/1,308 frames | encounter/action proof, remaining legacy low-detail sprites and production audio remain incomplete | texture groups, content registries and parallax composition | Audit the remaining legacy entity/item/UI sprites, then replace the highest-impact runtime set with the same provenance contract |
| Foreground tree silhouettes and biome identity | `Game.Core.World.Generation`, biome content | `verified` | Finite and infinite world generation share deterministic tree geometry and renderer-selected foliage atlases | `TreeSilhouettePlanner`, `TreeMaterialResolver`, `TreeTileVisualSelector`, `TreeGenerationStep`, `InfiniteWorldChunkGenerator` | 28/28 focused tree/worldgen/cache tests plus 3/3 strict foliage assets | Twelve seed/anchor-stable silhouettes combine root flare, stepped branches, varied crowns and negative openings; oak, pine and birch resolve through three 16-mask foliage sets without per-tree planner allocation | generation and visual selection are deterministic; tile atlas reuse avoids per-frame texture creation | biome comparison capture matrix and richer trunk/decor overlays remain open | tile registry and biome profiles | Capture Forest/Amber/Marsh comparisons and add bounded trunk/decor overlays without regressing atlas batches |
| Mining/building/interaction transactions | `Game.Core.Interaction`, `GameSimulation`, client feedback | `verified` | `BuildingSystem` delegates active placement to `BuildingPlacementTransactionService`; selected item use and targeting resolve typed candidate/validation/commit results | query/hold/mining tuning, candidate factory, building validator/system/transaction service | active building, atomic inventory/world, negative-X, mining and feedback suites | Faster global mining tuning, pass-through mineable `mangrove_root`, collision/support/reach checks and optimistic/authoritative inventory transactions pass | no calibrated interaction CPU trace | furniture anchors, background walls, liquids, rollback IDs and server request validation remain open | world edits, inventory and item definitions | Add versioned authoritative interaction request/rollback IDs, then furniture/wall/liquid placement rules |
| Inventory/crafting transactions | `Game.Core.Inventory`, `Game.Core.Crafting`, client overlays | `verified` | Active sideview inventory and crafting UI | transaction/query/organization/batch/search/tracking services | focused inventory/crafting/save contracts plus full suite | Atomic batch craft, max craft, favorites, cursor return, equipment preflight and typed result events pass locally | recipe refresh now revision-cached; no UI trace baseline | Recipe unlock/pin session persistence and overlay interaction automation remain incomplete | item definitions, save service and UI adapter | Persist knowledge/pins and add overlay input tests |
| Save/load round trips | `Game.Core.Saving` | `verified` | Session/world persistence | saving/session services, `SimulationSaveService`, `RandomStateSaveService`, `WorldEventStateSaveService` | save/migration/hash/RNG/event recovery tests | Negative X, player v1-v3, simulation time, RNG and world-event sidecars round-trip; world metadata V3 persists generation version/profile and missing legacy fields select V1; event state uses atomic replace, backup recovery, future-version rejection, legacy defaults and registry normalization for removed mods | save time/bytes unknown | region overwrite recovery, save rotation and user-facing corruption reports remain incomplete | content registries and filesystem | Add interrupted region-write recovery, integrity reports and save rotation |
| Reusable 2D movement and physics | `Game.Core.Movement`, `Game.Core.Physics` | `verified` | Intent-only sideview control plus central player/enemy/item bodies, swept tile collision, deterministic AABB narrowphase, material impulses/friction and overlap queries | movement controllers, `PhysicsWorld`, `EntityPhysicsRuntime`, `TileCollisionResolver`, broadphase and contracts | controller boundary, capacity rejection, entity migration, mutual masks, deterministic contacts/order and 500/1,000-body performance gates | Character intent is separated from physics integration; EntityManager submits complete fixed-capacity batches; pair/contact overflow fails before mutation; initial tile overlaps are recovered deterministically and reported | central 500-actor update median p99 1.688 ms versus retained 4.449 ms baseline; 1,000 bodies 0.988 ms and broadphase 0.331 ms, both 0 B; packed 500-body collision gate is 0 B | one-way platforms, slopes, joints, continuous body-body collision and liquid forces remain open | tile collision and fixed update | Add tile-shape/material contracts, joints and continuous fast-body pair tests |
| Active-cell liquid simulation | `Game.Core.World.Liquids` | `verified` | Authoritative world simulation tracks bounded active and deferred cell frontiers | `LiquidSimulationSystem`, `LiquidSimulationWorkspace`, scheduler integration | flow correctness, negative-X/infinite-world, active frontier, scan/recovery budget and performance contracts | Steady updates avoid full-region scans; initial and compatibility regions seed under a hard budget; scheduler world rebinding and capacity-recovery regions are explicit | 128 active cells p50/p95/p99 0.021/0.047/0.069 ms and 0 B/step; compatibility probe p99 14.597 ms | pressure/equalization, multiple liquid materials and presentation coupling remain open | tile mutations and scheduler | Activate directly from mutations, then add pressure/equalization and surface snapshots |
| Finite generation workspace | `Game.Core.World.Generation` | `verified` | Finite terrain/cave/ore/wall/liquid/tree/structure passes share chunk-local bounds-safe mutation storage | `WorldGenerationWorkspace`, mutation helpers and generation steps | workspace bounds/materialization/dirty-state, unchanged hashes and 256x128 performance gate | Initial generation bypasses runtime dirty propagation and publishes clean chunks only after all passes complete | latest quick fixture 0.694 ms average/0.862 ms p95 and 1,252,512 B, down from 28.044 ms/17,052,112 B | regional/infinite generation distributions and larger-world memory curves remain open | finite world and generation profiles | Capture 1K/4K world distributions and extend the workspace contract to regional jobs |
| Shared Terraria-style surface topology V2 | `Game.Core.World.Generation`, saving/session bootstrap | `verified` | New finite and infinite worlds share broad deterministic surface and soil sampling; existing infinite saves retain their original generator | `WorldSurfaceSampler`, `TerrainGenerationStep`, `InfiniteWorldChunkGenerator`, `WorldGenerationVersions`, world metadata V3 | multi-seed slope/plateau/relief, finite/infinite coordinate parity, coherent dirt, legacy sampler and save-migration tests plus full suites | 192/64/20-tile quintic surface bands create broad hills, valleys and plateaus with at most one tile local slope in the gate; the spawn sanctuary is flat and the 72/24-tile soil bands remove per-column dirt flicker. Metadata persists generator version/profile; V2 applies only to new worlds and metadata without a version loads as V1 | pure samplers are allocation-free by construction; larger infinite topology distributions remain unmeasured | cave networks, lakes and biome transition elevation still use older separate paths; legacy saves without a stored profile fall back to the selected profile | profile registry, saves, streaming and local-surface resolvers | Add shared chunk-cell cave worms, depression-planned lakes and 32-seed topology distributions without changing V1 generation |
| Caller-owned combat spatial queries | `Game.Core.Combat`, `Game.Core.Entities` | `verified` | Projectile/melee/contact resolution and homing reuse indexed workspaces | `CombatQueryWorkspace`, `EntityQueryWorkspace`, entity spatial index | swept-hit correctness/ordering, dense homing budget/fairness and 500-projectile performance fixture | Projectile hits use swept broadphase and deterministic time-of-impact order; homing rotates fairly under fixed query/candidate/entry-test limits with nearest-first selection | 500 projectiles average 0.290 ms, p99 0.532 ms and 0 B across 180 resolutions | body-sized projectile shapes and dense multi-faction distributions remain open | entity manager and spatial index | Add adversarial dense-scene distributions and terminal feedback integration |
| Benchmark executable | `Game.Benchmarks` | `verified` | CI quick smoke, local calibration and BenchmarkDotNet categories | stable harness, phase allocation export and `EngineBenchmarks.cs` | executable quick/calibration, streaming BDN dry/short, immutable query and lighting runs | Latest quick fixed tick is 0.116 ms average/0.159 ms p95, 0.261 ms p99 and 3,125 B average; calibration remains 0.102 ms/2,690 B; typed events remain 0 B and replay matches | compact streaming dry allocations are 13,576,584 B cold/7,475,264 B warm; lighting remains 40 B for 4/12 chunks | dry/short timing modes are not interchangeable; no backend GPU benchmark or automatic baseline-comparison gate yet | stable fixtures | Add threshold comparison in CI and service-phase distributions |
| Performance budgets | Engineering control plane | `partial` | Acceptance decisions | `PERFORMANCE_BUDGETS.md`, benchmark/smoke artifacts | evidence review | Snapshot, renderer counters, cold/warm streaming, 500-entity, active-liquid, generic physics, finite-generation and combat-query evidence are current; unknown fields remain explicit | see budget document | save, GPU timing/memory, large regional generation and advanced-liquid distributions remain unknown | telemetry and representative fixtures | Measure large-region generation, liquid pressure and dense adversarial combat scenes |
| Durable resume contract | Repository control plane | `verified` | Every future `Weiter` session | `AGENTS.md`, `WORK_LEDGER.md` | document consistency review | Ledger names one exact next action and validation set | not a performance metric | Requires disciplined updates after every session | source-of-truth order | Resume at Epic 2 Milestone 5 without replanning |
